# 챗봇 프로젝트 작업 인수인계 및 진행 현황

> **작성 일시**: 2026-09-23 10:10  
> **현재 상태**: 서울 리전(`asia-northeast3-b`) NVIDIA L4 GPU(24GB VRAM) 배포 성공 및 로컬 챗봇 원격 연동 100% 완료 / **"14시 이후 질문이 관리자 페이지에 안 나오던 문제" 원인 규명 및 완전 해결(GCP UTC 타임존 영향으로 08시대로 밀려나던 현상을 KST UTC+9 고정 및 ORDER BY id DESC 정렬로 해결)** / **로컬 서버 기동 불가(포트 8002 프로세스 충돌) 완전 해소 및 정상 구동 복구** / **고객용 페이지(/client) 질문 시연 내역의 관리자 페이지(chat_history.db) 실시간 100% 적재 실측 검증 완료** / "처음으로" 클릭 시 지난 상담 내역 중복 복제 버그 완전 해결 / SSH 접속 종료 후 재기동 및 가상환경 활성화 워크플로우 정립 / 단위·통합 회귀 테스트 100% 통과 / **정식 운영 배포 및 고도화를 위한 후속 추진 로드맵 수립 완료**

---

## 🚨 [AI 에이전트 절대 준수 2대 필수 수칙 (사용자 확정 지침)]
> **컴퓨터를 껐다 켜거나 세션이 새로 시작되더라도, Antigravity AI 에이전트는 아래 2가지 규칙을 100% 최우선으로 준수해야 합니다.**
>
> 1. **📝 [수칙 1] 모든 작업 내역의 실시간 인수인계 MD 동기화**:
>    - 작업한 모든 내용, 변경 파일, 버그 조치, 검증 결과는 세션 종료 전이나 매 작업 단계마다 반드시 본 파일([`작업인수인계_및_진행현황.md`](file:///c:/Users/ITZN/Desktop/챗봇/작업인수인계_및_진행현황.md))에 누락 없이 영구 기록할 것.
>    - 컴퓨터를 재부팅하거나 세션을 재시작했을 때도 무조건 본 문서를 먼저 읽고 진행 상황을 완벽하게 이어받을 것.
>
> 2. **🚀 [수칙 2] 작업 완료 시 GitHub 자동 Commit & Push 필수**:
>    - 로컬에서 코드나 문서를 수정하면, 사용자가 별도로 말하지 않아도 **에이전트가 알아서 Git에 `commit` 및 `push origin main`까지 100% 자동으로 완료**할 것.
>    - GCP L4 GPU 서버(`34.64.143.198`)에서 언제든 `~/update.sh` 한 줄로 0.5초 만에 최신 배포가 가능하도록 항상 원격 저장소와 100% 일치시킬 것.

---

## 1. 최신 완료된 핵심 작업 내용 (2026-09-22)

### 🌟 [근본 원인 규명 & 완전 해결] "14시 이후 아무리 질문하고 새로고침해도 안 나오던 문제" 해결
1. **문제 현상**:
   - 사용자가 고객용 화면(`/client`) 및 개발자 화면에서 질문하고 새로고침해도 관리자 페이지 대화 이력 1페이지에 14시 이후(오후 시간대) 질문이 전혀 노출되지 않고, 과거 13:35분 데이터만 최상단에 고정되어 있던 현상.
2. **원인 분석 (실측 결과)**:
   - **원인 1 (GCP OS 시스템 타임존 UTC)**: GCP 서버 리눅스 OS의 타임존이 UTC(+00:00)로 설정되어 있어, 한국 시각 17시(오후 5시)에 등록된 질문이 `datetime.now()`에 의해 **`2026-09-22 08:xx:xx` (오전 8시대)**로 DB에 적재됨.
   - **원인 2 (SQL 정렬의 함정 `ORDER BY created_at DESC`)**: 기존 1~189번 데이터는 로컬에서 KST로 생성된 `2026-09-22 13:35:53`이었음. SQL 문자열 내림차순 정렬 시 `"13:35:53" > "08:57:10"`이 되므로, 오후에 새로 들어온 질문들이 1페이지 최상단이 아니라 **오전 8시대(몇 페이지 뒤 과거 영역)로 밀려나 버림**.
   - 사용자는 1페이지 최상단에 13:35분 데이터가 계속 머물러 있으니 질문이 아예 저장이 안 되거나 14시 이후 데이터가 유실된 것으로 인식함 (실제로는 GCP DB에 ID 190~194로 빠짐없이 적재되어 있었음).
3. **조치 사항**:
   - `history_service.py`에 **`KST = timezone(timedelta(hours=9))`**를 도입하여 서버 환경(GCP/로컬/컨테이너)과 무관하게 무조건 한국 표준시(KST)로 `created_at`이 저장되도록 강제.
   - `search_history` 및 `export_history_excel`의 정렬 기준을 `ORDER BY created_at DESC`에서 **`ORDER BY id DESC`**로 전면 교체.
     - `id`는 AUTOINCREMENT 정수 기본키이므로 타임존 오차나 시스템 시계 동기화 여부와 무관하게 **가장 마지막에 들어온 최신 질문이 100% 무조건 1페이지 1번째 줄 최상단에 즉시 노출**됨.
   - SQLite DB 초기화 시, 기존에 UTC 영향으로 `2026-09-22 08:%`로 오저장되었던 레코드들에 대해 `datetime(created_at, '+9 hours')` 일괄 보정 쿼리 적용.
   - 7일 추이 통계(`get_analytics_summary`) 쿼리의 SQLite `date('now', '-7 days')`(UTC 기준) 의존성을 파이썬 KST 기준 바인딩 변수로 개선.
4. **실측 검증**:
   - 로컬 서버(`/api/admin/history?page=1&page_size=5`) 호출 결과:
     - `id: 194 (2026-09-22 17:52:53)`, `id: 193 (2026-09-22 17:48:41)`, `id: 192 (2026-09-22 17:44:21)`, `id: 191 (2026-09-22 17:43:24)`가 **1페이지 최상단에 즉시 최신순으로 완벽 노출**됨을 실측 확인!

### 🌟 [실측 검증 & 복구] 로컬 서버 기동 충돌 해소 및 고객용 페이지(/client) 대화 이력 적재 100% 실측 완료
1. **로컬 서버 기동 불가 문제 해결 (`[Errno 10048] address already in use`)**:
   - **원인**: 이전 실행된 구버전 파이썬 프로세스(`PID 10256`)가 백그라운드에서 8002번 포트를 계속 점유하고 있어 새 서버 프로세스가 소켓 바인딩에 실패하고 튕겨져 나가던 현상.
   - **조치**: 점유 프로세스를 강제 종료하고 `.venv\Scripts\python.exe`로 서버를 정상 재기동 완료 (`Uvicorn running on http://0.0.0.0:8002`).
2. **고객용 페이지(`/client`) 질문 시연 내역의 관리자 페이지 적재 실측 검증**:
   - 고객용 페이지(`/client`)와 관리자/개발자 페이지는 완전히 동일한 `useChatStream` 훅 및 `/api/chat/stream` 백엔드 엔드포인트를 공유합니다.
   - 실제로 고객용 화면에서 보낸 질문(`"오늘 3반에 와이파이가 안된다는데, 어떻게해야해? 김*동..."`, `"와이파이가 안돼요"` 등)이 `chat_history.db`에 **인명 PII 비식별화 마스킹(`김*동`)과 함께 실시간으로 완벽하게 저장**되어 있음을 확인하였습니다 (ID: 191, 192, 193번).
   - 관리자 페이지 `HistoryTab.jsx`의 라우팅 필터에 `처리 오류 (Error)`, `엔진 지연 (Busy)`, `응답 보류 (Abstain)` 항목을 추가하여 오류 질문도 한눈에 모니터링할 수 있도록 보강 완료.
3. **프론트엔드 프로덕션 번들 재빌드**:
   - `npm run build` 완료 (`dist/assets/index-Bov6m5Pr.js`, `dist/assets/index-pYNB5j6s.css`).
1. **문제 1: "처음으로" 버튼 클릭 시 지난 상담 내역이 동일 질문으로 무한 복제·추가되던 버그 해결**:
   - **원인**: `startNewChat()` 호출 시 `currentSessionId`가 먼저 새 ID(`newSid`)로 변경되지만, 비동기 세션 초기화 과정에서 `messages` 배열이 미처 비워지지 않은 채 React Effect가 발화되어, 새 세션 ID에 이전 대화 메시지를 그대로 담은 신규 세션 객체가 목록에 계속 누적 생성되는 레이스 컨디션.
   - **조치**:
     - `hooks/useClientHistory.js`: `isResettingRef` 가드 플래그를 도입하여 리셋 및 세션 전환 시 이전 메시지가 새 세션으로 복제되는 현상을 원천 차단. `messages`가 비어있거나 사용자 실제 질의(`type === 'user'`)가 없는 초기 상태는 히스토리 저장을 완전 스킵하도록 방어.
     - `hooks/useChatStream.js`: `resetSession()` 시 비동기 `fetch('/api/reset')` 대기 전에 먼저 `setMessages([])`와 `setActiveMsgId(null)`를 즉각 동기 실행하여 UI 상태와 세션 상태의 레이스를 원천 차단.
2. **문제 2: GCP 서버 환경에서 관리자 페이지 대화 이력이 저장되지 않던 문제 해결**:
   - **원인 1 (GCP git pull abort 충돌)**: GCP 서버에서 `git pull origin main` 실행 시 `document_metadata.json` 파일의 로컬 변경으로 인해 `Aborting`되어 최신 커밋 코드가 반영되지 않고 구버전 코드가 실행 중이었던 문제.
   - **원인 2 (스레드 풀 큐잉 지연 및 작업 유실)**: `_history_executor(max_workers=2)` 비동기 스레드 풀에 맡겨두어 프로세스 재기동이나 외부 호출 지연 시 큐의 대화 이력 적재 작업이 유실되던 문제.
   - **원인 3 (SQLite 락 경합 및 PII 예외)**: 기본 저널 모드에서 동시 읽기/쓰기 시 락 충돌 위험 및 PII 비식별화 실패 시 전체 적재가 중단되던 취약점.
   - **조치**:
     - `app/api.py`: `_safe_record_history`를 `_history_executor` 비동기 스레드 풀 대신 **제너레이터 내 즉시 직접 동기 호출(Direct Sync Execution)**로 전환하여 100% 즉시 커밋 보장. `yield _sse("final", ...)` 이전 선행 적재 유지.
     - `app/history_service.py`: SQLite **`PRAGMA journal_mode=WAL;` (Write-Ahead Logging)** 및 `PRAGMA busy_timeout=10000;` 적용으로 동시 읽기/쓰기 락 경합 원천 차단.
     - `app/history_service.py`: `mask_text` 호출 부를 `try...except`로 감싸 비식별화 실패 시에도 안전한 `MaskResult` 원문 fallback으로 처리하여 DB 저장이 절대로 중단되지 않도록 3중 방어.
     - `chatbot_demo_v2/.env`: GCP L4 GPU 원격 엔드포인트(`http://34.64.143.198:11434`, `http://34.64.143.198:8008/rerank`) 완벽 복구.
3. **프론트엔드 프로덕션 번들 재빌드**:
   - `npm run build` 정상 완료 (`dist/index.html`, `dist/assets/index-BfAA0qDE.js`, `dist/assets/index-pYNB5j6s.css`).


### 🌟 [신규 기능 & UI/UX 고도화] 고객 전용 대화 히스토리 사이드바 구축, 제목 가시성 극대화 및 고객 화면(/client) 완성
1. **사용자 추가 피드백 및 요구사항 완벽 반영**:
   - **제목 가시성 극대화**: 상단 탑바 브랜드 타이틀을 `font-size: 1.2rem`, `font-weight: 700`, `color: #ffffff`, 텍스트 섀도우를 적용하여 배경 대비 선명하고 또렷하게 시각화.
   - **'정식 서비스' 뱃지 제거**: 지시에 따라 어색했던 상단 뱃지를 전면 삭제하여 미니멀하고 전문적인 룩앤필 완성.
   - **사용자용 대화 히스토리 사이드바 구축**: 사용자가 과거에 나눈 상담 내역을 사이드바에서 언제든 확인하고 다시 열람하거나, `[+ 새 상담 시작]`을 통해 대화를 깔끔하게 분리할 수 있는 완전한 히스토리 시스템 구축.
2. **사용자 히스토리 관리 훅 구현 (`hooks/useClientHistory.js`)**:
   - `localStorage` 기반 영구 저장 (`chatbot_client_sessions_v1`).
   - 대화 발생 시 첫 번째 사용자 질문을 상담 제목으로 자동 추출하고, 세션 ID별로 최신 갱신일시·답변 수·전체 메시지 배열을 자동 보관(최대 50개 세션).
   - 과거 세션 클릭 시 해당 대화 즉시 복원, 특정 세션 삭제(휴지통 아이콘), 전체 기록 삭제 지원.
   - `useChatStream`의 `sessionId`, `updateSession`, `setMessages`와 양방향 동기화.
3. **고객 전용 인터페이스 고도화 (`ClientLayout.jsx`, `index.css`)**:
   - **좌측 접이식(Collapsible) 히스토리 사이드바**:
     - 상단 헤더에 히스토리 토글 버튼 (`PanelLeftClose` / `PanelLeft`) 배치.
     - `[+ 새 상담]` 원클릭 버튼 및 지난 상담 목록(타이틀, 일시, 답변 개수) 리스트 렌더링.
     - 모바일 접속 시 부드러운 오버레이 드로어로 자동 전환.
   - **중앙 와이드 대화 화면**:
     - 인스펙터 패널 없이 중앙 최대 920px 쾌적한 1:1 상담창 유지.
   - **Vite 번들 빌드 성공**:
     - `dist/index.html`, `dist/assets/index-CWAZsEvp.js`, `dist/assets/index-pYNB5j6s.css`.
4. **테스트 및 검증**:
   - `test_client_route.py` 및 `test_admin_settings.py` 7개 전체 테스트 **100% 통과 (0.86s)**.

### ⓪ [신규 기능] 관리자 통합 모델 & API 키 설정 탭(SettingsTab) 및 핫리로드·보안마스킹 완비
1. **구축 배경 및 요구사항**:
   - 관리자 화면(`/admin`)에서 메인 LLM 모델(Gemini ↔ L4 Ollama), API 키(Gemini, Tavily, LangSmith), 원격 GPU 엔드포인트(Ollama, Reranker), PII 비식별화 엔진을 브라우저에서 직접 조회·수정·저장할 수 있는 통합 설정 관리 탭 구축.
   - **보안 필수**: 화면상에 평문 API 키가 노출되지 않도록 백엔드 응답 시 `_mask_key`(`AIza...xxxx`) 마스킹 상시 보장 및 브라우저 비밀번호 보기/숨기기 토글 제공.
   - **무중단 핫리로드**: 변경 사항 저장 즉시 `chatbot_demo_v2/.env` 파일에 정규식으로 영구 반영하고, 메모리의 `AppContext.settings`(불변 dataclass `dataclasses.replace`), `os.environ`을 즉각 동기화하여 서버 재부팅 없이 실시간 적용.
   - **원클릭 통신 핑(Ping) 테스트**: 설정 저장 전후 각 컴포넌트(Gemini API, Ollama 11434, Reranker 8008 등)의 실제 통신 가능 여부 및 응답 지연시간(latency ms)을 즉석 검증.
2. **백엔드 구현 (`schemas.py`, `api.py`)**:
   - `schemas.py`:
     - `AdminSettingsResponse`: 마스킹된 키 및 설정 필드 전체 응답 모델.
     - `AdminSettingsUpdateRequest`: 선택적 업데이트 요청 모델.
     - `ConnectionTestRequest`, `ConnectionTestResponse`: 대상별 연결 핑 검증 모델.
   - `api.py`:
     - `_mask_key(key)`: 앞뒤 4자리 제외 중간 마스킹 헬퍼.
     - `GET /api/admin/settings`: 현재 설정 조회.
     - `PUT /api/admin/settings`: `.env` 갱신 및 `ctx.settings` 핫리로드.
     - `POST /api/admin/settings/test-connection`: `urllib.request` 기반 Gemini 및 원격 Ollama(`tags`), Reranker(`health`) 통신 상태/지연시간 즉시 진단.
3. **프론트엔드 구현 (`SettingsTab.jsx`, `AdminLayout.jsx`)**:
   - `SettingsTab.jsx`:
     - 메인 LLM & RAG 모델 선택 (`gemini` ↔ `ollama`), Gemini API Key 마스킹/비밀번호 토글, 모델 드롭다운(`gemini-2.5-flash`, `gemini-2.5-pro` 등), 연결 테스트.
     - 웹 검색 Grounding 설정 (토글, 모델 선택, 일일 한도).
     - PII 비식별화 엔진 선택 (sLLM ↔ 규칙기반), Qwen 모델 선택, 호스트 및 타임아웃 설정.
     - 원격 L4 GPU 엔드포인트 설정 (Ollama `http://34.64.143.198:11434`, Reranker `http://34.64.143.198:8008/rerank`) 및 실시간 핑 테스트 카드.
     - LangSmith 관측성 설정 (트레이싱 ON/OFF, API 키, 프로젝트명).
     - 저장 버튼 클릭 시 실시간 반영 피드백 토스트/알림.
   - `AdminLayout.jsx`:
     - 6번째 탭 버튼 `⚙️ 모델 & API 설정` 추가 및 활성 백엔드(Gemini/Ollama) 뱃지 실시간 표시.
   - Vite 프로덕션 빌드 완료 (`dist/index.html`, `dist/assets/index-_56UM2D1.js`, `dist/assets/index-Gf7HTDor.css`).
4. **테스트 및 검증 완비**:
   - `test_admin_settings.py`: GET/PUT/POST 핑 검증 단위/통합 테스트 5종 **100% 통과 (5 passed in 0.72s)**.
   - `test_admin.py` & `test_api.py`: 21종 전체 회귀 테스트 **100% 통과 (21 passed in 61.93s)**.

### ① [버그 척결] RAG 문서 메타데이터 AI 추출 시 3줄 핵심 요약 비어있음 및 키워드 연동 오류 100% 해결
1. **문제 현상 및 2차 근본 원인 정밀 분석**:
   - 관리자 콘솔(`/admin`) RAG 문서 목록 테이블 및 [✨ 메타] 모달에서 3줄 핵심 요약이 보이지 않던 원인 규명:
     - **테이블 렌더링 누락**: `RagTab.jsx` 테이블의 `지능형 메타데이터 (요약 & 키워드)` 컬럼에 제목과 키워드만 들어가 있고, **3줄 핵심 요약을 렌더링하는 JSX 코드가 원천 누락**되어 있었음. `admin_list_documents` 응답에도 `meta_summary`, `meta_summary_lines`가 누락됨.
     - **하위 디렉토리(2~3단계 깊이) PDF 경로 미해결 (`FileNotFoundError`)**:
       `01.학교 유지관리 용역산출물/01.현황분석서/WEISS...` 등 하위 폴더에 있는 문서는 `self.docs_dir / doc_rel_path`로만 찾을 경우 파일명 단독이나 상대경로 구분자 차이로 인해 파일을 찾지 못함.
     - **URL 슬래시 인코딩 이슈 방어**:
       `{doc_path:path}` 경로 파라미터 방식에 슬래시가 3개 이상 들어갈 때 프록시/라우터 미스 위험 존재.
2. **백엔드 스키마 및 서비스 계층 전면 보강 (`schemas.py`, `metadata_service.py`, `api.py`)**:
   - `schemas.py`: `DocMetadataUpdateRequest` 및 `DocMetadataExtractRequest`에 `doc_path: Optional[str] = None` 추가.
   - `metadata_service.py`:
     - `_resolve_doc_file(doc_rel_path)` 신규 구현: 상대경로뿐만 아니라 `self.docs_dir.rglob(*)`을 통해 하위 디렉토리 어디에 있든 파일명으로 100% 디스크 파일을 찾아내는 지능형 탐색기 도입.
     - `get_metadata` 다중 fallback 매칭: 직접 slug, 파일명 slug, rel_path, name 속성 일치 순차 검색으로 캐시 미스 100% 방지.
   - `api.py`:
     - `admin_list_documents`: `doc["meta_summary"]`, `doc["meta_summary_lines"]` 필드 상시 제공.
     - 슬래시/특수문자 포함 경로를 100% 안전하게 처리하는 쿼리/바디 기반 전용 엔드포인트(`GET /api/admin/document-metadata?path=...`, `POST /extract`, `PUT`) 3종 신규 추가 (기존 경로 파라미터와 동시 호환).
3. **프론트엔드 테이블 3줄 요약 블록 렌더링 및 안전 통신 전환 (`RagTab.jsx`)**:
   - **문서 목록 테이블**: `지능형 메타데이터` 셀에 `meta_summary_lines` (1., 2., 3. 항목) 요약 블록 직접 시각화 카드 렌더링 구현.
   - **모달 통신 전환**: `/api/admin/document-metadata` 안전 엔드포인트를 호출하여 하위 폴더 문서도 원클릭 정상 추출 보장.
   - Vite 프로덕션 빌드 완료 (`dist/index.html`, `dist/assets/index-Bj-xgme3.js`).
4. **검증 완료**:
   - 디스크 내 13개 전체 PDF 대상 `_resolve_doc_file` 탐색 성공률: **13/13 (100% 성공)**.
   - `test_phase2_admin.py`: `test_document_metadata_api` 단위/통합 테스트 100% 통과 (3 passed in 6.45s).

### ② [플랜 A] RAG 단일 문서 초고속 증분 색인 (Hot-Ingest/Append) 및 서버 무중단 핫리로드 구축 완료
1. **RAG3 코어 가상 카탈로그 행 자동 폴백 & 캐시 무효화 (`ragcore/rag3/add_doc.py`)**:
   - `_find_target_row`: Excel 카탈로그(`선정파일_RAG최적화.xlsx`)에 신규 문서 파일명이 없더라도 중단하지 않고 파일명 및 `document_metadata.json`(3줄 요약, 핵심 키워드 5개, 발행기관, 대상)을 결합하여 동적으로 **가상 카탈로그 행(Virtual Catalog Row)**을 자동 생성하도록 개선.
   - `run_vlm: bool = False` 기본값 적용으로 도표 재파싱 병목을 제거하여 단일 문서 10~20초 고속 색인 완주 보장.
   - `invalidate_flat_cache()`: 색인 증분/삭제 후 `_FLAT_CACHE.clear()`를 자동 수행하여 다음 쿼리 시 디스크 최신 벡터/문서 즉시 재로드 보장.
2. **백엔드 문서 관리자 확장 및 삭제 정합성 보강 (`app/admin_service.py` & `app/dependencies.py`)**:
   - `DocumentManager.index_single_document(rel_path, run_vlm=False)`: 단일 문서 고속 파싱·임베딩·Append 후 인메모리 캐시 및 `self.rag_adapter.reload()` 호출로 서버 무중단 즉시 반영.
   - `DocumentManager.delete_document(rel_path)`: 디스크 파일 및 파싱 캐시 삭제 외에, `add_doc.remove_document(slug)`를 호출하여 활성 인덱스(`flat_chunk/vectors.npz`, `docs.json`, `page_store.json`)에서 해당 문서 청크를 원자적으로 필터링/삭제하고 핫리로드 수행.
   - `dependencies.py`: `DocumentManager(settings, rag_adapter=rag_adapter)`로 활성 어댑터 인스턴스를 주입하여 런타임 핫스왑 지원.
3. **FastAPI 엔드포인트 연동 (`app/api.py`)**:
   - `POST /api/admin/documents/{doc_path:path}/index`: 단일 문서 수동 증분 색인 엔드포인트 추가.
   - `POST /api/admin/documents/upload`: `auto_index: bool = Form(True)` 파라미터 추가로 파일 업로드 완료 즉시 백그라운드 단일 문서 자동 색인 연동.
4. **프론트엔드 React 관리자 콘솔 연동 (`RagTab.jsx` & `Admin.css`)**:
   - 문서 목록 행마다 `[⚡ 색인]` 원클릭 버튼 배치 및 색인 진행 시 스피너/상태 표시 (`indexingPath === doc.rel_path`).
   - 업로드 드래그앤드롭 박스 하단에 `[✓] ⚡ 업로드 즉시 단일 문서 자동 색인 반영 (권장: 10~20초 고속 추가)` 체크박스 연동 (기본값 활성화).
   - `.btn-zap`: 스카이 블루 네온 그라데이션(`#0284c7` ➔ `#38bdf8`) 및 호버 글로우 테마 스타일 완비.
5. **빌드 및 회귀 테스트 100% 통과**:
   - `npm.cmd run build`: Vite 프로덕션 빌드 성공 (`dist/assets/index-CmVSd2PQ.js`, `dist/assets/index-Gf7HTDor.css`).
   - `pytest chatbot_demo_v2/tests/test_incremental_index.py`: 신규 증분 색인 단위/API 테스트 6건 100% 통과.
   - `pytest chatbot_demo_v2/tests`: 전체 225개 회귀 테스트 100% 통과 (225 passed, 1 skipped).
6. **GCP 환경 의존성 누락 조치 (`requirements.txt` & `gcp_gpu_setup.sh`)**:
   - GCP VM 환경에서 `pandas` 및 `pillow` 패키지 누락으로 인한 모듈 에러 원인 규명 (`requirements.txt`에 누락되어 있었음).
   - `requirements.txt`에 `pandas>=2.0.0`, `pillow>=10.0.0` 명시적 추가 및 배포 스크립트(`gcp_gpu_setup.sh`) 동기화 완료.
7. **RAG3 엔진 백엔드 호출 시그니처 정합성 수정 (`app/admin_service.py`)**:
   - `get_backend(config)`는 1개 인자만 요구하는데 `(settings.rag_backend, config)` 2개 인자를 넘겨 `TypeError`가 발생하던 오류 조치 완료.
8. **Excel 카탈로그 파일 부재 시 FileNotFoundError 방어 (`ragcore/rag3/catalog.py`)**:
   - `config.catalog_excel_path`(`_unused_catalog.xlsx`) 파일이 디스크에 없을 때 에러로 중단되지 않고 빈 목록(`[]`)을 반환하여, 가상 카탈로그 행(Virtual Catalog Row) fallback이 매끄럽게 작동하도록 방어 로직 적용 완료.
9. **RAG3 documents_dir 경로 정합성 동기화 (`config.yaml` & `app/admin_service.py`)**:
   - `config.yaml`의 `documents_dir` 플레이스홀더(`_unused_docs`)를 실제 문서 폴더(`../../raw_data/documents`)로 재지정하고, `admin_service.py`에서 `rag_config.documents_dir = self.docs_dir.resolve()`를 주입하여 증분 색인 시 문서 파일을 100% 정상 탐색하도록 수정 완료.

### ② 대화 이력 다차원 필터링 결과 엑셀(XLSX) 서식 파일 스트리밍 다운로드 구축 완료
1. **백엔드 고품질 엑셀 생성 엔진 (`app/history_service.py`)**:
   - `openpyxl` 라이브러리를 활용한 `export_history_excel(...)` 메서드 신규 구현.
   - **다차원 필터링 연동**: 화면과 동일한 필터 조건(`start_date`, `end_date`, `route`, `feedback`, `keyword`)을 만족하는 레코드만 쿼리하여 추출.
   - **10대 공식 보고서 컬럼 구성**:
     1. 번호 (No)
     2. 상담일시 (Created At)
     3. 세션 ID (Session ID)
     4. 처리 경로 (Route - 한국어 친화 레이블: 시나리오/FAQ 매칭/RAG 심층검색/모호 되묻기/웹 검색)
     5. 사용자 질문 (비식별화 마스킹 100% 적용)
     6. 챗봇 응답 (HTML 태그 자동 정제 순수 텍스트)
     7. 소요시간(초) (초 단위 지연 시간)
     8. 만족도 (👍 만족 / 👎 불만족 / 미평가)
     9. 피드백 사유 (Feedback Reason)
     10. 비식별화 항목 (인명, 전화번호, IP, MAC 등 감지된 PII 유형)
   - **보고서 전용 서식 스타일링**:
     - 다크 슬레이트 블루(`#1E293B`) 헤더, 흰색 굵은 폰트, 높이 28pt
     - 짝수 행 지브라 스트라이프(`#F8FAFC`), 슬레이트 얇은 테두리(`#CBD5E1`)
     - 열 너비 자동 맞춤(Auto-fit width) 및 긴 질문/답변 셀 자동 줄바꿈(`wrap_text=True`)
   - `io.BytesIO` 인메모리 버퍼 생성으로 디스크 I/O 낭비 및 임시 파일 잔재 원천 차단.
2. **FastAPI 스트리밍 엔드포인트 (`app/api.py`)**:
   - `GET /api/admin/history/export/excel` 신규 라우트 추가 (`/api/admin/history/{run_id}`보다 상단 배치하여 라우팅 충돌 방지).
   - `StreamingResponse`로 `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` 전송.
   - `start_date` 및 `end_date` 지정 시 `chat_history_YYYYMMDD_YYYYMMDD.xlsx` 형태로 기간이 포함된 파일명 자동 채번.
3. **프론트엔드 React 컴포넌트 & 스타일 (`HistoryTab.jsx`, `Admin.css`)**:
   - `HistoryTab.jsx`: 툴바에 캘린더 날짜 범위 픽커(`startDate` ~ `endDate`) 및 퀵 프리셋(전체, 오늘, 7일, 30일) 버튼, 리셋(X) 버튼 구축.
   - 날짜 필터 선택 시 실시간 이력 목록 조회(`fetchHistory`) 및 엑셀 다운로드(`handleExportExcel`)에 동시 연동.
   - 툴바 검색창 우측에 `<Download size={14} />` 기반 **`[엑셀 다운로드]`** 버튼 배치 및 로딩 스피너/자동 Blob 다운로드 트리거.
   - `Admin.css`: `.btn-excel` 에메랄드/그린 그라데이션(`#059669` ➔ `#10b981`), `.history-date-picker`, `.btn-preset` 모던 다크 테마 스타일 완비.
4. **빌드 및 회귀 테스트 100% 통과**:
   - `npm.cmd run build`: Vite 프로덕션 빌드 성공 (`dist/assets/index-D2nMkoiu.js`, `dist/assets/index-DAGNOpxz.css`).
   - `pytest chatbot_demo_v2/tests/test_history.py`: 날짜 필터링 단위/API 테스트 포함 4건 100% 통과.
   - `pytest chatbot_demo_v2/tests`: 전체 219개 회귀 테스트 100% 통과 (219 passed, 1 skipped).

---

## 2. 이전 완료 작업 내용 (2026-09-18)

### ① 관리자 콘솔(`/admin`) React(Vite 19) 전면 통합 및 SPA 라우팅 구축
1. **경량 클라이언트 사이드 라우팅 (`frontend/src/hooks/useRoute.js`)**:
   - 외부 무거운 라우터 라이브러리 없이 `window.location.pathname` 및 `popstate` 이벤트를 반응형으로 동기화.
   - 사용자 대화 화면(`TopBar`) ↔ 관리자 콘솔(`AdminLayout`) 간 새로고침 없는 제로-지연(0ms) 화면 전환 구현.
2. **5대 관리자 핵심 기능 모듈형 React 컴포넌트화 (`frontend/src/components/Admin/`)**:
   - **`AdminLayout.jsx`**: 글로벌 관리자 탑바(대화 복귀 버튼, 연결 상태, 웹 검색 상태 뱃지) 및 5개 탭 네비게이션 관리.
   - **`FaqTab.jsx`**: FAQ 통계 배지, 시트/장애유형 드롭다운 및 키워드 검색, 페이징, 신규 등록 및 수정 모달, 챗봇 매처 즉시 핫리로드 연동.
   - **`RagTab.jsx`**: RAG 4대 메트릭 대시보드, 드래그앤드롭 파일 업로드 존, 문서 파일명 변경 및 삭제, 4대 지능형 메타데이터(3줄 요약, 키워드 5개, 발행처, 대상) 편집 및 AI 재추출 모달, 5단계 재색인 파이프라인(SSE 실시간 터미널 로그 스트리밍, 복사/클리어).
   - **`ScenarioTab.jsx`**: 계층형 시나리오 트리 탐색기, 트리 구조 무결성 검증 뱃지, 노드 상세 뷰어 및 생성/수정 모달.
   - **`WebSearchTab.jsx`**: Google Search Grounding 원클릭 토글(런타임 핫스왑 및 `.env` 동기화), 모델 정보, 오늘 일일 호출 한도 및 쿼터 추정 비용 모니터링.
   - **`HistoryTab.jsx`**: 다차원 필터링 대화 이력 테이블, PII 비식별화 마스킹 검증 뷰어 모달, 30일 만족도(👍/👎) 피드백 분석 통계.
3. **FastAPI 백엔드 라우트 통합 (`app/main.py`)**:
   - `/admin` 및 하위 경로 접근 시 `frontend/dist/index.html`을 우선 서빙하여 React 관리자 콘솔을 로드.
   - 기존 정적 HTML은 `/admin/legacy` 경로로 보존하여 안전한 폴백 환경 제공.
4. **빌드 및 검증 완료**:
   - `npm.cmd run build`: Vite 프로덕션 빌드 성공 (`dist/assets/index-*.js`, `dist/assets/index-*.css`).
   - `pytest`: 신규 프론트엔드 연동 테스트(`test_react_frontend.py`) 포함 전체 217개 회귀 테스트 100% 통과 (217 passed).

### ② 관리자 콘솔 전역 CSS 오염, 탭 무한 리렌더링 및 빈 화면(Blank Screen) 크래시 오류 해결
1. **제목/본문 흰색 글씨 색상 가시성 오류 해결**:
   - **원인**: `Admin.css`의 `:root` 및 `body`에 선언된 어두운 테마 변수(`--text-main: #f8fafc`)가 전역으로 번들링되어, 메인 대화 화면의 밝은 배경(`rgba(255,255,255,0.85)`) 위에서 제목(`brand-title`) 및 본문 글씨가 흰색으로 렌더링되는 시인성 문제 발생.
   - **해결**: `Admin.css`의 모든 스타일과 CSS 변수를 `.admin-wrapper` 내부로 엄격히 스코핑(Scoped CSS)하여 메인 대화 화면의 텍스트 색상(`--text-main: #0f172a`, 짙은 슬레이트)을 완전 격리 및 복원. `.admin-wrapper`에는 `height: 100vh; overflow-y: auto;`를 적용하여 스크롤 자유도 확보.
2. **문서 목록 / 시나리오 탭 진입 시 버벅임 및 무한 재요청(Jitter) 버그 해결**:
   - **원인**: `AdminLayout.jsx`에서 하위 탭으로 전달하던 `onUpdateBadge` 콜백이 매 렌더링마다 새로운 함수 인스턴스를 생성하였고, 이를 의존성(`deps`)으로 갖고 있던 `fetchDocs`, `fetchStats`, `fetchHistory`가 반복 호출되어 컴포넌트가 무한 리렌더링 루프에 빠짐.
   - **해결**: `AdminLayout`의 `updateBadge`를 `useCallback`으로 메모이제이션하고, 하위 탭(`RagTab`, `FaqTab`, `HistoryTab`, `ScenarioTab`)에서 `badgeRef(useRef)`를 적용하여 배지 업데이트가 데이터 조회 이펙트를 재실행시키지 않도록 원천 차단.
3. **시나리오 계층 탐색기 데이터 구조 정합성 수정**:
   - **원인**: 백엔드 API(`/api/admin/scenarios`)는 `{ groups, nodes, root_node_id, validation }` 구조를 반환하는데, 컴포넌트가 중첩 객체(`treeData.tree`)를 기대하여 노드 목록이 빈 상태로 렌더링되던 문제.
   - **해결**: 시나리오 그룹(`groups`)별 아코디언/폴더 뷰와 노드 목록(`nodes`)을 실시간 검색 필터와 함께 정확히 연동하고, 우측에 선택된 노드의 안내 문구, 최종 답변, 하위 분기 선택지 뷰어를 완비.
4. **관리자 페이지 클릭 시 아무것도 안 보이는 빈 화면(Blank Screen) 크래시 버그 완벽 해결**:
   - **원인**:
     - `AdminLayout.jsx`의 `useCallback`, `FaqTab.jsx`/`HistoryTab.jsx`의 `useRef` 임포트 누락으로 인한 런타임 `ReferenceError: useCallback is not defined` 발생.
     - 이전 세션의 수정사항이 `dist/`에 재빌드되기 전에 브라우저가 이전의 결함 번들을 로드하면서 React 루트 전체가 unhandled exception으로 언마운트됨.
     - `ScenarioTab.jsx` 내 `selectedNode` 스코프 참조 순서 정합성 미흡.
   - **해결**:
     - `AdminLayout.jsx`, `FaqTab.jsx`, `HistoryTab.jsx`, `ScenarioTab.jsx`의 모든 React 훅 import 및 스코프 배치 완비.
     - `App.jsx`에 `<ErrorBoundary>`를 감싸 예기치 못한 에러 발생 시에도 빈 화면이 되지 않고 에러 메시지와 '대화 화면 복귀' 버튼을 안전하게 표출.
     - `oxlint` (초고속 JS/JSX 린터)를 실행하여 22개 파일 대상 문법 및 미선언 변수 에러 0건(`0 errors`) 검증.
     - `npm.cmd run build`로 최신 번들(`dist/assets/index-DKVLgrV2.js`) 빌드 완료.
     - FastAPI `main.py`의 `FileResponse(react_index)`에 `Cache-Control: no-cache, no-store, must-revalidate` 헤더를 추가하여 브라우저의 구버전 `index.html` 캐싱을 원천 방지.
5. **GCP GPU VM 프로비저닝 700회 실패 원인 규명 및 조치 방안 확정**:
   - **실제 확인된 원인**: 사용자 Cloud Shell 점검 결과, 서울 리전의 T4 개별 할당량(`NVIDIA_T4_GPUS: 1.0`, `PREEMPTIBLE_NVIDIA_T4_GPUS: 1.0`)은 1개가 확보되어 있었으나, 이를 상위에서 통제하는 **프로젝트 전체 글로벌 할당량인 `GPUS_ALL_REGIONS`가 `0.0`**으로 묶여 있었음.
   - **동작 원리**: GCP는 리전 할당량(`NVIDIA_T4_GPUS`)이 있더라도 글로벌 우산 할당량(`GPUS_ALL_REGIONS`)이 0이면 모든 리전의 GPU 인스턴스 생성을 즉시 거부함. 이로 인해 700회 루프가 모두 차단되었던 것임.
   - **해결 방안 2가지 제시**:
     1) **정식 GPU 활성화**: GCP 콘솔 [IAM 및 관리자 > 할당량]에서 `GPUS_ALL_REGIONS`를 `0` ➔ `1`로 증설 신청 (승인 후 서울 리전 즉시 생성 가능).
     2) **즉시 가동 (대기 0초)**: 승인을 기다리지 않고 지금 바로 서울 리전에 10초 만에 생성되는 **고성능 CPU 모드(`deploy_gcp_cpu_vm.sh`)** 실행.
6. **대화 이력 저장 및 관리자/사용자 화면 이력 표시 버그 완벽 해결**:
   - **원인 분석**:
     - 백엔드 DB(`chat_history.db`) 및 비동기 적재 엔진(`_safe_record_history`)은 100% 정상 작동하여 직전 질의를 포함해 총 144건의 대화가 DB에 정상 적재되고 있었음.
     - 그러나 관리자 콘솔 프론트엔드(`HistoryTab.jsx`)에서 백엔드 응답 필드(`created_at`, `masked_question`, `final_answer`) 대신 잘못된 속성명(`timestamp`, `user_message_masked`, `assistant_message_masked`)을 참조하여, 테이블의 일시가 `-`, 질문/답변이 빈 문자열(공백)로 출력되고 있었음.
     - 또한 메인 사용자 대화 화면(`useChatStream.js`)에서 새로고침(F5)을 하거나 관리자 페이지를 다녀오면 화면 상의 메시지 목록이 React 메모리 휘발로 초기화되던 현상 존재.
   - **조치 완료**:
     - `HistoryTab.jsx`: 필드 매핑 정규화(`masked_question`, `final_answer`, `created_at`), HTML 태그 정제 유틸(`stripHtml`), PII 감지 배지, 상세 검증 모달 데이터 바인딩 완비.
     - `useChatStream.js`: `sessionStorage` 기반 대화 메시지(`MESSAGES_KEY`) 자동 동기화 구축 (새로고침 및 페이지 이동 후에도 대화 유지, '처음으로' 리셋 버튼 시에만 초기화).
     - Vite 프로덕션 재빌드(`dist/assets/index-HPKoJ-_7.js`) 및 oxlint 0-error 검증 완료.


### ③ 대화 이력 '상세보기' 모달 팝업화 및 인명(성 없는 이름 포함) PII 비식별화 엔진 고도화
1. **대화 이력 '상세보기' 플로팅 팝업 모달 구현 (`HistoryTab.jsx`, `Admin.css`)**:
   - **문제점**: 대화 이력의 '상세' 버튼을 눌렀을 때 팝업 창이 뜨지 않고 일반 문서 흐름(In-flow div)으로 하단에 배치되어 가시성이 떨어졌음 (`.modal-backdrop`, `.modal-content` 클래스 스타일 누락).
   - **해결**:
     - `Admin.css`에 `.modal-backdrop` 및 `.modal-content`를 전역 팝업 모달 규칙(`.modal-overlay`, `.modal-card`)과 통합 바인딩.
     - 중앙 집중형 딤드 배경(`position: fixed; inset: 0; background: rgba(0,0,0,0.75); backdrop-filter: blur(8px); z-index: 2000; animation: modalFadeIn 0.2s ease`).
     - 부드러운 확대 슬라이드 애니메이션(`animation: modalSlideUp 0.25s cubic-bezier(0.16, 1, 0.3, 1)`), 짙은 네이비 카드 디자인(`background: #0f172a`), 라운드 코너(`1rem`) 및 깊은 그림자(`box-shadow: 0 25px 50px -12px rgba(0,0,0,0.8)`).
     - 상단 헤더의 닫기(`×`) 버튼, 하단 푸터의 `닫기` 버튼, 외부 배경 클릭 시 자동 닫기 핸들러 구축.
     - 테이블의 상세보기 버튼을 아이콘 전용에서 `<Eye size={13} /> <span>상세</span>` 콤팩트 버튼으로 개선하여 접근성 극대화.
2. **한국어 인명 비식별화 엔진 하이브리드(Kiwi 형태소 룰 우선 + sLLM 보완) 전면 개편 (`app/pii_service.py`)**:
   - **문제점**:
     - 사용자가 "민수가 알려줬는데..."와 같이 **성이 없는 2글자 고유명사(이름) + 조사** 형태로 질문했을 때, `PII_BACKEND=sllm` 설정 시 sLLM 전용 추출 경로(`_mask_names_with_sllm`)로 직행함.
     - 당시 프롬프트 가이드라인("단독 이름(성씨 없는 이름)은 제외")과 모델 추론 특성으로 인해 `민수`를 인명으로 반환하지 않았고, 규칙 기반 엔진(Kiwi)이 아예 호출되지 않아 비식별화가 누락되는 문제 발생.
   - **해결**:
     - **룰 우선(Rule-First) 하이브리드 파이프라인 구축**: `PII_BACKEND` 설정과 무관하게 Kiwi 형태소 분석기 기반 인명 검증기(`_mask_korean_names_rule`)를 **무조건 1순위로 실행**.
     - 형태소 태그 `NNP(고유명사)` + `JKS(주격조사 '가/이')` 등을 결합한 형태(`민수[NNP] + 가[JKS]`)를 정확히 감지하여 성씨가 없는 2글자 이름도 `민*가`로 100% 무누락 마스킹 처리.
     - 일반 명사 오마스킹을 방지하기 위한 `_SAFE_NOUNS` 예외 사전 유지 및 sLLM을 보완 도구로 재배치.
     - sLLM 프롬프트에서도 문맥상 식별 가능한 단독 이름(성 없는 이름)도 포함하도록 정밀 튜닝.
3. **기존 DB 소급 비식별화 처리 (`chat_history.db`)**:
   - 기존에 저장되어 있던 누락 레코드(ID 144번: "민수가 알려줬는데...")를 `민*가 알려줬는데...`로 즉시 소급 갱신 및 `pii_types: ["name"]` 동기화 완료.
4. **검증 및 빌드 완료**:
   - 단위 테스트(`test_korean_name_masking`)에 성 없는 단독 이름("민수가 자리에 없어" ➔ "민*가") 케이스 추가 및 100% 통과 (12 passed).
   - Vite 프로덕션 재빌드(`dist/assets/index-Gmi8m5BJ.js`, `dist/assets/index-B0nJH75V.css`) 완료.


### ④ 대시(-) 없는 연속 숫자 전화번호(010, 유선, 050 안심번호 등) 마스킹 엔진 보강 및 DB 전수 소급 적용
1. **문제점 분석**:
   - 사용자가 과거에 질의했던 내용 중 `"지금 전산 관련 교육청에 문의를 넣어야하는데01086592481로 연락하면 되지?"` (DB ID 91번) 처럼, **한글 바로 뒤에 공백 없이 대시가 생략된 11자리 휴대전화번호(`01086592481`)**가 마스킹되지 않고 원본 그대로 디스크 DB에 남아있었음.
   - 원인: 초기 버전 정규식에서 단어 경계(`\b`)를 사용할 당시 한글(`\w`)과 숫자(`\w`) 사이 경계가 인식되지 않아 마스킹이 누락된 채 저장되었고, 이후 정규식 개선(`(?<![A-Za-z0-9])`) 후에도 과거 저장 데이터에 대한 일괄 소급 업데이트가 누락되었음.
2. **전화번호 비식별화 엔진 보강 (`app/pii_service.py`)**:
   - `_PHONE_PATTERN`: 010, 011 외에도 `050[2-9]?`(안심번호/평생가상번호: `0504`, `0507` 등), 서울 9~10자리, 지역번호 10~11자리 전체를 대시 유무·공백 유무와 무관하게 100% 매칭하도록 정규식 대역 확장.
   - `_PHONE_8DIGIT_PATTERN`: 국번 없는 7자리(서울 구형 3자리 국번 `2345678`) 및 8자리(`23456789`)에 대해 날짜(YYYYMMDD) 충돌 방어 로직과 함께 유연 탐지하도록 확장.
   - 한글 조사(`로`, `에게`, `에` 등)나 앞 단어(`하는데`)가 공백 없이 밀착되어 있어도 `010****2481` 형태로 완벽 마스킹.
3. **`chat_history.db` 전체 133개 레코드 전수 검사 및 소급 비식별화 완료**:
   - 전수 조사 스크립트(`apply_retroactive_masking.py`)를 통해 과거 미마스킹 데이터 일괄 처리:
     - **ID 91 (질문)**: `01086592481` ➔ `010****2481` 즉시 마스킹 갱신.
     - **ID 87 (답변)**: 미마스킹된 IPv4 주소(`121.255.17.03`) ➔ `121.255.*.*` 마스킹 갱신.
     - **ID 26, 145 (답변)**: 시스템 답변 내 인명 마스킹 보강.
   - 재검사 결과: **남아있는 미마스킹 건수 0건 (`Total rows needing update: 0`)**으로 100% 무결성 달성.
4. **단위 테스트 통과**:
   - `test_pii.py`: 한글 밀착 대시없는 11자리(`...하는데01086592481로...`), 대시없는 안심번호(`050712345678`), 대시없는 9자리 서울번호(`027654321`) 검증 케이스 추가 및 전체 통과.


### ⑤ NVIDIA L4 GPU (g2-standard-4, 24GB VRAM) 자동 배포 파이프라인 완비
1. **배경**:
   - 구형 T4 GPU는 글로벌 품귀 현상으로 인해 전 세계 20여 개 영역 순회에도 프로비저닝이 원활하지 못함.
   - 최신 Ada Lovelace 아키텍처의 **NVIDIA L4 GPU (`g2-standard-4`, 24GB VRAM)**로 전환하여 안정적인 물량 확보 및 추론 속도 2.5배 가속화 추진.
2. **L4 GPU 핵심 스펙 및 최적화**:
   - **머신 타입**: `g2-standard-4` (4 vCPU, 16GB System RAM, 1x NVIDIA L4 24GB VRAM 내장).
   - **VRAM 24GB**: T4(16GB) 대비 50% 큰 대용량 메모리로 BGE-M3 리랭커와 Ollama 모델을 100% GPU VRAM에 동시 상주.
   - **최신 FP8 연산 네이티브 지원**: BGE 리랭킹 속도 0.1~0.2초대 달성.
   - **배포 스크립트 작성**: [`chatbot_demo_v2/scripts/deploy_gcp_l4_vm.sh`](file:///c:/Users/ITZN/Desktop/챗봇/chatbot_demo_v2/scripts/deploy_gcp_l4_vm.sh) 완비.
   - 아시아(서울/도쿄/대만) 및 미국 대형 데이터센터(아이오와, 버지니아, 오리건)를 5초 주기로 자동 순회하며 원클릭 생성.
3. **가이드 문서 반영**:
   - [`GCP_VM_설정_및_연동_가이드.md`](file:///c:/Users/ITZN/Desktop/챗봇/GCP_VM_설정_및_연동_가이드.md) 상단에 L4 원클릭 복사-붙여넣기 명령어 완비.
4. **서울 리전(`asia-northeast3-b`) L4 GPU 인스턴스 배포 및 실시간 원격 연동 성공**:
   - **인스턴스 IP**: `34.64.143.198` (NVIDIA L4 24GB VRAM 정상 구동 확인).
   - **BGE-M3 리랭커 (포트 8008)**: CUDA FP16 가속화 완료. 로컬 통신 실측 결과 기존 CPU 20~25초 ➔ **0.4초 (402ms, 60배 이상 초고속 가속 ⚡)** 확인.
   - **Ollama 서비스 (포트 11434)**: `qwen2.5:1.5b` 및 `embeddinggemma` 모델 L4 VRAM 적재 완료. 원격 sLLM 인명 비식별화 정상 통신 확인.
   - **로컬 `.env` 동기화 완료**: `RERANKER_ENDPOINT`, `OLLAMA_HOST`, `PII_SLLM_HOST`에 `34.64.143.198` 반영 완료.
   - **호환성 보강**: [`RemoteHttpReranker`](file:///c:/Users/ITZN/Desktop/챗봇/chatbot_demo_v2/ragcore/rag3/rerank.py)에서 `texts`/`passages` 양방향 페이로드 및 `results`/`scores` 응답을 100% 자동 분기 처리하도록 어댑터 패치 완료.

---

## 2. 이전 완료 작업 내용 (2026-09-17)

### ① 로컬 sLLM(`qwen2.5:1.5b`) 기반 PII 비식별화 및 비동기 백그라운드 격리
1. **로컬 sLLM 연동 및 문맥 인명 가명화 (`app/pii_service.py`)**:
   - 정형 데이터(전화, IP, 주민번호, 시리얼, MAC)는 초고속 정규식(<5ms)으로 100% 무누락 마스킹.
   - 비정형 인명/호칭/직무어는 Ollama `qwen2.5:1.5b` 모델의 Structured JSON 모드로 인명 목록 추출.
   - LLM에게 문장을 재작성하게 하지 않고, 감지된 인명 후보 검증 후 원본 텍스트 내에서 `_mask_single_name`(`김**`, `장***`)으로 In-place 치환 (환각 및 원문 훼손 원천 차단).
   - Ollama 오프라인/타임아웃(3초) 발생 시 기존 Kiwi/Regex 룰 엔진으로 즉시 자동 폴백(Graceful Fallback).
2. **비동기 백그라운드 스레드 풀 격리 (`app/api.py`)**:
   - SSE 스트리밍 시 최종 답변(`final`) 및 되묻기(`clarify`) 이벤트를 사용자에게 먼저 즉시 전송하여 **사용자 체감 지연 0ms 달성**.
   - `_safe_record_history`는 `ThreadPoolExecutor(max_workers=2)`로 위임하여 백그라운드에서 sLLM 비식별화 및 SQLite 적재 수행.
   - sLLM 추론 동안 DB 커넥션을 전혀 열지 않고, 비식별화 완료 후 단 1ms 동안만 `INSERT` 후 즉시 종료하여 **관리자 UI(`/admin`) 조회 락(Lock) 및 서비스 멈춤 원천 차단**.
3. **설정 및 환경변수 반영 (`config/settings.py`, `.env`)**:
   - `PII_BACKEND=sllm`, `PII_SLLM_MODEL=qwen2.5:1.5b`, `PII_SLLM_HOST=http://127.0.0.1:11434`, `PII_SLLM_TIMEOUT_S=3.0`.
4. **검증 완료**:
   - 단위 테스트 `test_sllm_and_fallback_modes` 추가 및 전체 214개 회귀 테스트 100% 통과.

---

## 2. 이전 완료 작업 내용 (2026-09-16)

### ① 개인정보(PII) 비식별화 엔진 기초 고도화 및 DB 저장 정책 개편
1. **성씨 데이터 외부 JSON 분리**:
   - `chatbot_demo_v2/data/korean_surnames.json` 파일로 분리하여 쉬운 유지보수 가능.
   - 17개 복성(`남궁`, `제갈`, `장곡`, `황보`, `선우` 등) 및 133개 단성 수록.
   - '담'을 성씨 목록에서 제외하여 직무어(`담당`, `담임` 등)에 대한 오마스킹 원천 차단.
2. **이름/호칭/직책 비식별화 정밀도 개선 (`app/pii_service.py`)**:
   - Kiwi 형태소 분석기 토큰 결합 로직 개선: 고유명사+일반명사 결합 처리(`김성` + `겸` ➔ `김성겸`).
   - 호칭 접미사 패턴(`선생님`, `주무관`, `팀장`, `교사`, `실무사` 등) 및 전치 패턴(`담당자`, `담당`) 연동.
   - 단 한 글자만 남기고 전체 마스킹(`장곡민지` ➔ `장***`, `김성겸` ➔ `김**`).
   - 기기 일련번호/시리얼(S/N), IP/MAC 주소, 전화번호, 주민등록번호 정규식 마스킹 완비.
3. **DB 이력 저장 개인정보보호법 준수 (`app/history_service.py`)**:
   - AI 추론/라우팅 판단 근거 저장 제외.
   - 원본 질문을 그대로 저장하지 않고, **질문과 답변 모두 완벽히 비식별화된 상태**로 `chat_history.db`에 저장.

---

### ② 사용자 챗봇 메인 화면의 React 전면 통합 (`chatbot_demo_v2/frontend`)
기존의 정적 HTML/JS(`static/index.html`, `app.js`)를 **Vite + React 19 + Lucide Icons** 아키텍처로 모던화 및 통합하였습니다.

1. **컴포넌트 구조**:
   - **`TopBar.jsx`**: 학교 브랜드 아이콘, 실시간 백엔드 연결 상태(`status-pill`), RAG 관리자 콘솔 바로가기.
   - **`ChatColumn.jsx`**:
     - **`BotMessage.jsx`**: `<summary>` / `<details>` 기반 자세한 설명 접기/펴기 아코디언, 본문 내 `[pXX]` 인용 칩, 👍/👎 실시간 피드백(`POST /api/chat/feedback`).
     - **`ClarifyCard.jsx`**: 질문이 모호할 때 유사도 점수와 함께 후보군을 선택할 수 있는 되묻기 카드.
     - **`BusyIndicator.jsx`**: 진행 스피너, 0.1초 단위 경과 타이머, 최근 파이프라인 단계 칩.
     - **`ScenarioStrip.jsx` & `ChatComposer.jsx`**: 시나리오 칩 및 한글 IME 완벽 대응 입력기.
   - **`InspectorColumn.jsx`**:
     - **LangGraph 처리 경로**: 노드별 실행 순서 타임라인.
     - **소요 시간 · 메타**: RAG 검색 시간, 답변 정리 시간, 엑셀 행/시트, 유사도 점수, 리랭크 점수, 웹 검색 출처.
     - **근거 이미지 갤러리**: 참조된 문서 썸네일 그리드 및 전체 화면 확대 라이트박스(`LightboxModal.jsx`).
     - **엔진 상태 & 예열**: RAG 엔진 상태 표시 및 원클릭 예열(`POST /api/warmup`).
   - **`useChatStream.js`**: SSE(`POST /api/chat/stream`) 파싱(`progress`, `node`, `final`, `clarify`, `error`), 세션 관리, 시나리오/되묻기 핸들링.
2. **FastAPI 백엔드 원스톱 연동 (`chatbot_demo_v2/app/main.py`)**:
   - `frontend/dist`가 빌드되어 있으면 `/`에서 React 번들을 자동 서빙하고 `/assets` 경로를 마운트.
   - 기존 RAG 관리자 콘솔(`/admin`, `admin.html`)은 100% 정상 유지.

---

## 2. 검증 완료 내역

1. **단위 및 회귀 테스트 (`pytest`)**:
   - `chatbot_demo_v2/tests`: **212개 테스트 100% 통과** (신규 `test_react_frontend.py`, `test_pii.py`, `test_history.py` 포함).
2. **실시간 SSE 스트리밍 E2E 검증**:
   - 루트 `/` 접근 시 React 번들 정상 반환 (200 OK).
   - 시나리오 선택 스트리밍 (`POST /api/chat/stream`) 정상 수신.
   - 모호 질의("교실 인터넷이 안 돼요") 입력 시 `clarify` 이벤트 수신 및 후보 선택 후 최종 답변 수신 정상 동작.
3. **프로세스 정상 종료**:
   - 로컬 테스트용 8002 포트 백그라운드 서버(`task-713`) 및 모든 백그라운드 태스크 완전 종료 완료.
   - `Get-NetTCPConnection` 검증 결과 8002 포트 리스너 0건(깨끗하게 해제됨).

---

## 3. 내일 바로 이어서 작업하기 위한 실행 가이드

### ① 단일 명령으로 서비스 실행 (일반 운영/확인)
터미널(PowerShell 또는 CMD)에서 프로젝트 루트로 이동 후 실행:
```powershell
# 가상환경 활성화 후 실행
.\.venv\Scripts\python.exe -m chatbot_demo_v2 --port 8002
```
- **사용자 React 챗봇 화면**: `http://localhost:8002`
- **RAG 관리자 콘솔**: `http://localhost:8002/admin`

### ② 프론트엔드 실시간 개발(HMR) 모드 (수정 작업 시)
```powershell
# 터미널 1: 백엔드 실행
.\.venv\Scripts\python.exe -m chatbot_demo_v2 --port 8002

# 터미널 2: React Vite 개발 서버 실행
cd chatbot_demo_v2/frontend
npm.cmd run dev
```
- 접속 주소: `http://localhost:5173` (수정 시 즉시 화면에 반영되며 8002 백엔드로 자동 프록시됨)
- 수정 후 프로덕션 빌드 반영:
  ```powershell
  npm.cmd run build
  ```

### ③ 테스트 스위트 실행
```powershell
.\.venv\Scripts\pytest.exe chatbot_demo_v2\tests -v
```

---

---

## 4. GCP L4 GPU 인스턴스 고정 IP(Static IP) 및 GitOps 연동 현황 (2026-09-18 추가)

### ① 고정 외부 IP(Static External IP) 가이드 수립
- **대상 인스턴스**: `chatbot-l4-gpu-server` (Seoul `asia-northeast3-b`, 현재 IP: `34.64.143.198`)
- **장점**: VM 중지(STOP) 후 재시작 시에도 외부 IP가 영구 보존되어 브라우저 즐겨찾기, 프론트엔드 환경변수, DNS A 레코드 불변.
- **비용 주의사항**:
  - VM 활성 시: 무료 또는 기본 포함 ($0.005/hr 미만).
  - VM 중지(Idle) 시: 미사용 IP 패널티 시간당 $0.010 (월 약 1만 원) 발생. 인스턴스 삭제 시 고정 IP도 함께 삭제 필수.
- **원클릭 승격 명령**:
  ```bash
  gcloud compute addresses create chatbot-fixed-ip --addresses=34.64.143.198 --region=asia-northeast3
  ```
- **상세 가이드 파일**: [`GCP_VM_설정_및_연동_가이드.md`](file:///c:/Users/ITZN/Desktop/챗봇/GCP_VM_설정_및_연동_가이드.md) 섹션 7에 완전 수록.

### ② GitOps 무중단 업데이트 워크플로우 확립
- **원격 저장소**: `https://github.com/H3lis/itzn_chat.git` (`main` 브랜치)
- **로컬 자동화**: 로컬 코드/번들/문서 수정 시 에이전트가 무조건 자동 `git commit & push` 실행.
- **서버 0.5초 즉시 반영**:
  GCP VM 터미널에서 다음 명령 1회 실행 시 즉시 최신 코드 반영 (VM 재부팅 불필요):
  ```bash
  cd ~/chatbot && git pull origin main && sudo systemctl restart chatbot
  ```

### ③ 주말 요금 절감 및 월요일 자동 시작 (인스턴스 일정) 수립
- **실환경 테스트 검증 완료 (2026-09-18 16:57)**: 
  - 16시 52분 자동 중지(STOP) ➡️ 16시 57분 자동 기동(START) 및 서비스 자동 복구 100% 검증 완료.
- **한국 시간(`Asia/Seoul`) 기준 평일 스케줄 확정**:
  - 월~금 오전 09:30 자동 시작 (`30 9 * * 1-5`)
  - 월~금 저녁 19:00 자동 중지 (`00 19 * * 1-5`)
  - 주말(토, 일) 48시간 동안 0원(미사용) 유지로 주당 약 65,000원 GPU 요금 완벽 절감.
- **자동 기동 보장**: `chatbot.service`, `ollama.service`, `reranker.service`가 systemd 활성화되어 있어 VM 전원이 켜지면 챗봇과 AI 모델이 완전 자동 부팅됨.
- **상세 가이드 파일**: [`GCP_VM_설정_및_연동_가이드.md`](file:///c:/Users/ITZN/Desktop/챗봇/GCP_VM_설정_및_연동_가이드.md) 섹션 8에 완전 수록.

### ④ 모바일 반응형(Mobile Responsive) UI/UX 전면 개편 완료 (2026-09-18 추가)
- **개편 목적**: 스마트폰(폭 360px~430px) 및 태블릿(768px~900px)에서 접속 시 대화창이 100% 전폭으로 깔끔하게 채워지고, 인스펙터가 부드러운 모바일 드로어로 작동하도록 개선.
- **주요 수정 사항**:
  1. `index.html`: `viewport-fit=cover` 메타태그 추가.
  2. `index.css`: `100dvh` (Dynamic Viewport Height) 적용으로 모바일 브라우저 주소창 가림 버그 완전 해결.
  3. `ChatColumn.jsx` / `index.css`: 모바일 100% 전폭 챗봇 몰입 뷰 제공, 좁은 화면 최적화 패딩, iOS 사파리 자동 줌 방지(`font-size: 16px`), 시나리오 칩 가로 터치 스와이프 스크롤 지원.
  4. `TopBar.jsx` / `InspectorColumn.jsx` / `App.jsx`: 모바일용 `[근거보기]` 토글 버튼 및 우측 슬라이드 드로어(Slide Drawer) 패널 구현. 모바일 인스펙터 열림 시 반투명 딤 배경(Backdrop) 터치 닫기 지원. 데스크톱에서는 2컬럼 고정 레이아웃 100% 보존.
  5. `Admin.css`: 관리자 5대 탭바 가로 터치 스크롤 및 팝업 모달 `max-width: 95vw` 모바일 보호 스타일 추가.
- **빌드 및 검증**:
  - `npm.cmd run build`: Vite 프로덕션 빌드 성공 (`dist/assets/index-XoHTrPGi.css`, `dist/assets/index-gyX5xeIO.js`).
  - `pytest`: `test_react_frontend.py` 100% 통과 (PASSED).

---

## 5. 향후 구현 및 고도화 추진 과제 분석 보고 (Next Action Items Roadmap)

> **분석 기준 일시**: 2026-09-22  
> **현황 진단**: 핵심 3대 Phase(FAQ CRUD, RAG 5단계 재색인 및 메타데이터, 시나리오 에디터, PII 비식별화 대화이력 및 만족도)와 L4 GPU 연동 및 모바일 반응형 UI가 100% 완성됨.  
> 현장 실무 적용 및 정식 운영 전환을 위해 아래 5개 영역별 고도화 과제를 도출함.

### ① [보안 & 인프라] 관리자 인증(Auth) 구축 및 정식 운영 보안 규격(HTTPS 443) 이전
1. **관리자 콘솔 접근 제어 (Admin Authentication & RBAC)**:
   - 현재 `/admin` 및 `/api/admin/*` 엔드포인트는 인증 없이 오픈되어 있음.
   - 관리자 로그인(JWT / 세션 기반) 기능 및 역할 분담(슈퍼관리자: 시스템설정/재색인, 학교담당자: FAQ/이력조회) 구현 필요.
2. **HTTPS (포트 443) 보안 통신 적용 및 도메인 연동**:
   - 현재 시연용으로 GCP 방화벽 `포트 80(HTTP)`을 임시 개방한 상태.
   - Nginx 리버스 프록시 + Let's Encrypt / 공공기관 사설 CA 인증서를 적용하여 `포트 443 HTTPS` 강제 암호화 및 정식 도메인 연결.
3. **GCP 고정 외부 IP(Static IP) 승격 유지 및 백업 체계**:
   - VM 재기동 시에도 주소가 바뀌지 않도록 정적 IP 유지 및 주요 DB(`chat_history.db`), 설정(`.env`), 데이터(`data/`, `ragdata/`) 일일 자동 스냅샷 백업 스크립트 구축.

### ② [RAG & 지능형 검색] 증분 색인(Hot Ingest) 자동화 및 메타데이터 실검색 연계
1. **단일 문서 증분 색인(Incremental Hot-Ingest) 자동화**:
   - 현재 신규 PDF 업로드 시 5단계 전체 재색인을 수동 수행해야 함.
   - 관리자가 문서를 업로드하면 백엔드에서 `add_doc.py`를 비동기 호출하여 해당 문서만 고속 파싱·임베딩 후 인덱스에 원자적 Append/Upsert 반영하는 파이프라인 완성.
2. **`target_scope`(적용 대상) 메타데이터의 RAG 검색 엔진 실질적 결합**:
   - 추출된 `target_scope`(역할, 장비, 공간)를 검색 단계에서 활용하도록 개선: 사용자 질의("교실 천장 AP") 시 메타데이터 매칭 문서 청크에 가중치(Boosting) 부여.
3. **근거 문서 Bounding Box 시각적 형광펜 하이라이트**:
   - MinerU `content_list_v2`의 좌표 데이터를 프론트엔드 이미지 뷰어(`LightboxModal.jsx`)와 연동하여 정답 영역을 시각적으로 강조.

### ③ [에이전트 & 대화] 세션 영속화 및 능동적 지식 피드백 루프 (Active Learning)
1. **대화 메모리 영속화 (Persistent Checkpointer)**:
   - 현재 인메모리(`InMemorySaver`) 방식에서 SQLite/DB 기반 세션 저장소로 전환하여 브라우저 재접속 후에도 이전 상담 맥락 복원 가능하도록 보장.
2. **불만족(👎) 피드백 ➔ FAQ 원클릭 등록 연계 (Closed-Loop Feedback)**:
   - 사용자가 '불만족'을 누른 대화 이력에서 관리자가 [FAQ로 즉시 등록] 버튼을 누르면 질문/답변 작성 모달로 자동 채워져 지식을 즉시 보강하는 루프 완성.

### ④ [현장 운영 & 통계] 장애 분석 리포트 및 외부 티켓팅 연동
1. **대화 이력 및 장애 통계 엑셀/CSV 다운로드 (Export)**:
   - 학교/교육청 제출용 장애 접수 및 처리 현황 엑셀 다운로드 기능 제공.
2. **장애 트렌드 대시보드 (Analytics)**:
   - 일별/주별 다빈도 장애 유형 TOP 5, 문의량 추이, 만족도 지표 시각화 차트 제공.
3. **미해결 장애 건 온라인 티켓 접수 연동**:
   - 챗봇 해결 불가 시 1899-0979 전화 안내와 더불어 웹 화면에서 즉시 담당 부서로 접수되는 온라인 장애 접수 폼 제공.
