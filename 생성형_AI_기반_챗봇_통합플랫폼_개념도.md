# 🏛️ 생성형 AI 클라우드 기반 챗봇 통합플랫폼 목표 개념도 (Target Architecture Blueprint)

본 문서는 **학교 유무선 장애상담 챗봇 및 지능형 관리자 통합 플랫폼**의 최종 목표 시스템에 대한 **전체 개념도(Target Architecture)**와 데이터 파이프라인, 그리고 **관리자 5대 핵심 기능(FAQ CRUD, RAG 문서 전처리 및 메타데이터, 웹 검색, 시나리오 편집, 비식별화 대화 이력)**을 종합 정리한 마스터 설계서입니다.

> 📌 **호스팅 및 인프라 운영 방식 명시 (현행 2가지 방법 + 향후 전환 계획)**:
> 1. **현재 운영/시연 호스팅 방식 (2가지 방식 제공)**:
>    - **[방법 1: 로컬 PC 단독 운영]**: 로컬 환경에서 챗봇 시스템 전체(WAS + LangGraph + 모델 + 데이터)를 단독 구동 (`포트 8002`, 폐쇄망 및 독립 검증 가능).
>    - **[방법 2: GCP GPU VM 단독 운영]**: GCP 클라우드 GPU 인스턴스(NVIDIA L4/T4)에서 챗봇 시스템 전체를 올인원 단독 구동 (BGE 리랭커 60배 초고속 가속).
>    - *(※ 로컬과 GCP를 쪼개어 통신하는 하이브리드 연동 방식은 네트워크 지연 및 통신 이슈로 기각되었습니다.)*
> 2. **GCP VPC 방화벽 포트 80 임시 개방**:
>    - GCP GPU 서버 운영 시 통신 및 인바운드 이슈로 인해 **`포트 80 (HTTP)`을 임시로 강제 개방하여 확인/시연 중**입니다.
> 3. **향후 정식 운영 인프라 전환**:
>    - 향후 **[정식 전용 운영 서버(공공기관/행정망 온프레미스 또는 전용 프라이빗 클라우드)]**를 배정받아 보안 규격(`포트 443 HTTPS`)에 맞추어 이전·전환할 계획입니다.
> 4. **FAQ 처리 방식**:
>    - FAQ는 관리자가 복잡한 수동 임베딩을 돌릴 필요 없이, **초기/현재 방식대로 관리자 콘솔에서 등록/수정/삭제 시 시스템이 자동 반영(즉시 핫리로드)하는 단순하고 직관적인 CRUD 구조**를 유지합니다.

---

## 1. 🏛️ 시스템 핵심 구성 및 상호작용 개념도 (User & Admin Core Architecture)

사용자 상담 트랙(왼쪽)과 관리자 통제 및 중앙 데이터 트랙(오른쪽)의 상호작용을 직관적으로 도식화한 핵심 아키텍처 블록도입니다.

```
┌───────────────────────────────┐                             ┌─────────────────────────────────────────────┐
│                               │                             │                                             │
│          사용자 UI            │                             │                                             │
│  • 교직원 대화창 (SSE 실시간) │                             │                                             │
│  • 상황별 장애 분기 버튼      │                             │                                             │
│  • 근거 문서 및 이미지 뷰어   │                             │                관리자 UI                    │
│  • 👍 / 👎 피드백              │                             │               (5가지 기능)                  │
└───────────────────────────────┘                             │                                             │
             ▲             ▲                                  │  [1] FAQ 관리 (CRUD 즉시반영)               │
             │             │                                  │  [2] RAG 문서/전처리 & 메타데이터 뷰어       │
             │ (답변 스트리밍) (참조 데이터 전달)              │  [3] 웹 검색 Grounding 실시간 On/Off        │
┌────────────┴──────────────────┐                             │  [4] 버튼형 시나리오 트리 에디터 (수정/등록)│
│                               │                             │  [5] 대화 이력 검색 & PII 비식별화 감사     │
│             챗봇              │ <─────── (정책/핫리로드 제어) ───────┤                                             │
│    (시나리오 / FAQ / RAG)     │                             │                                             │
│                               │                             │                                             │
│ • LangGraph 오케스트레이션    │                             └──────────────────────┬──────────────────────┘
│ • 1순위: 버튼 시나리오 분기   │                                                    │
│ • 2순위: FAQ 매칭 (1ms)       │ <── (데이터 갱신/배포) ──┐                          │ (비식별화 이력 저장/감사)
│ • 3순위: RAG 하이브리드 검색  │                         │                          │ (문서/메타데이터 중앙관리)
│ • 4순위: 웹 검색 (보조)       │                         │                          ▼
└───────────────────────────────┘                         │           ┌─────────────────────────────┐
             ▲                                            │           │                             │
             │ (1ms 초고속 인메모리 로드)                 │           │           서버 DB           │
┌────────────┴──────────────────┐                         │           │     (중앙 영구 저장소)      │
│                               │                         │           │                             │
│           로컬 DB             │                         │           │ • chat_history.db           │
│      (고속 런타임 저장소)     │ ────────────────────────┘           │   (PII 마스킹 암호화 이력)  │
│                               │                                     │ • raw_data/documents/       │
│ • data/faq.json (질답 원문)   │                                     │   (원본 PDF 매뉴얼 아카이브)│
│ • data/scenarios.json (트리)  │                                     │ • document_metadata.json    │
│ • FlatChunkIndex (vectors.npz)│                                     │   (LLM 추출 3줄요약/키워드) │
│ • 런타임 인메모리 정규화 행렬 │                                     │ • .env 영구 설정 파일       │
└───────────────────────────────┘                                     └─────────────────────────────┘
```

### 📋 5대 핵심 블록 상세 역할 정의

| 블록 명칭 | 물리적 위치 및 기술 스택 | 주요 구성요소 및 역할 |
|---|---|---|
| **1. 사용자 UI**<br>(Client UI) | • 브라우저 웹 (HTML/CSS/JS)<br>• Flutter 모바일 앱<br>• SSE (Server-Sent Events) | • 교직원의 질문 입력 및 실시간 타이핑 효과 수신<br>• 인터넷/와이파이/태블릿 고장 시 **버튼형 시나리오 선택**<br>• 신뢰도 확보를 위한 근거 문서 원본 이미지 뷰어 제공<br>• 답변 만족도(👍/👎) 원클릭 평가 |
| **2. 챗봇**<br>(시나리오 / FAQ / RAG) | • LangGraph 0.2+ 엔진<br>• Intent 라우터 (14노드)<br>• BGE 리랭커 & Ollama | • 사용자 질의 의도에 따라 **4단계 자동 라우팅**:<br>  - 1순위: 버튼 시나리오 트리 (상태 전이)<br>  - 2순위: FAQ 의미 매칭 (NumPy 코사인 1ms)<br>  - 3순위: RAG 문서 검색 (Kiwi BM25 + Dense RRF)<br>  - 4순위: Google Search Grounding 웹 검색<br>• 오타/구어체 정규화 및 환각 검증(Grader) 수행 |
| **3. 로컬 DB**<br>(고속 런타임 저장소) | • 런타임 메모리(RAM)<br>• 로컬 JSON 및 NPZ 압축 파일 | • 챗봇 엔진이 질의 응답 시 1ms 만에 초고속 참조하는 데이터:<br>  - `data/faq.json` (질답 236건 원문)<br>  - `data/scenarios.json` (버튼형 트리 구조)<br>  - `vectors.npz` + `docs.json` (RAG 청크 2,500개 압축 색인)<br>  - NumPy L2 정규화 인메모리 행렬 (`self._mat`) |
| **4. 관리자 UI**<br>(5가지 핵심 기능) | • FastAPI 기반 웹 콘솔<br>• 5대 탭 통합 인터페이스<br>(`/admin`) | • **[기능 1] FAQ 관리**: 질문·답변 CRUD (저장 시 자동 핫리로드)<br>• **[기능 2] RAG 문서 관리 & 전처리**: PDF 업로드, 파일명 수정(Rename), LLM 메타데이터(요약/키워드/적용대상) 뷰어, 5단계 재색인 콘솔<br>• **[기능 3] 웹 검색 설정**: 실시간 On/Off 토글 및 예산 추적<br>• **[기능 4] 시나리오 에디터**: 기존 장애 트리 수정, 버튼/노드 추가, 무결성 검증<br>• **[기능 5] 대화 이력 & 비식별화**: 일자별 캘린더 검색, PII 실시간 마스킹, 만족도 통계 대시보드 |
| **5. 서버 DB**<br>(중앙 영구 저장소) | • SQLite / PostgreSQL<br>• 파일 시스템 영구 볼륨<br>(GCP / 전용 서버) | • 관리와 감사를 위해 중앙에 영구 보관되는 데이터:<br>  - `chat_history.db`: 이름, 전화번호, IP가 **마스킹된 안전한 대화 이력**<br>  - `raw_data/documents/`: 업로드된 원본 PDF 매뉴얼 보관소<br>  - `document_metadata.json`: LLM이 추출한 3줄 요약/키워드 카탈로그<br>  - 환경 설정 및 접속 로그 감사 기록 |

### 🔄 블록 간 화살표 동작 시나리오
1. **`관리자 UI ➔ 로컬 DB` (데이터 갱신 및 배포)**: 관리자가 FAQ 수정, 문서 재색인, 시나리오 트리를 변경하면 로컬 데이터 파일 및 인메모리 행렬로 즉시 반영됩니다.
2. **`관리자 UI ➔ 챗봇` (정책 반영 및 핫리로드)**: 관리자의 변경 사항이 서버 재기동 없이 챗봇 메모리에 즉시 핫리로드(Hot-reload)되며, 웹 검색 On/Off 정책이 실시간 반영됩니다.
3. **`관리자 UI ➔ 서버 DB` (영구 적재 및 감사 로그)**: 일자별 대화 이력 조회, 비식별화된 데이터 영구 적재, 원본 PDF 및 메타데이터를 중앙 보관합니다.
4. **`로컬 DB ➔ 챗봇` (초고속 인메모리 참조)**: 사용자가 질문을 던졌을 때 외부 DB 네트워크 통신 없이 로컬 메모리(NumPy 행렬 및 FlatIndex)에서 1ms 만에 유사도를 비교합니다.
5. **`챗봇 ➔ 사용자 UI` (실시간 답변 스트리밍)**: SSE 스트리밍을 통해 실시간 타이핑 효과 및 상황별 버튼 분기 옵션을 제공합니다.
6. **`챗봇 ➔ 서버 DB` (비식별화 이력 적재)**: 사용자 상담 완료 시 PII 엔진이 개인정보(이름, 연락처, IP)를 마스킹한 후 `chat_history.db`에 안전하게 보관합니다.

---

## 2. 🏗️ 전체 통합 아키텍처 상세 구조도 (Detailed System Blueprint)

```
=========================================================================================================================================================================
사용자 (클라이언트)  │          UI / 관리자 포털         │      AI Agent & 오케스트레이션       │        GPU / AI 모델 서빙         │           데이터 레이어           │       네트워크 및 인프라
=========================================================================================================================================================================
                     │  [ 대화형 포털 (사용자) ]         │  [ LangGraph 메인 에이전트 ]         │  [ AI 모델 서빙 (로컬 / GCP) ]    │  [ 지식 저장소 (문서/시나리오) ] │  [ 호스팅 인프라 (2가지 운영 방식) ]
 [ 교직원 / 사용자 ] │  ┌─────────────────────────────┐  │  ┌────────────────────────────────┐  │  ┌─────────────────────────────┐  │  ┌─────────────────────────────┐  │  ┌─────────────────────────────┐
        🧑‍🏫          │  │ • 장애 상담 대화창 (SSE)    │  │  │ • 14노드 오케스트레이션        │  │  │ • Docker: Ollama 서빙       │  │  │ • raw_data/documents/ (PDF) │  │  │ [방법 1] 로컬 PC 단독 운영   │
                     │  │ • 버튼형 시나리오 선택      │  │  │ • Intent 라우터 (FAQ/RAG/시나리오)│  │ • BGE Reranker 서버 (:8008)  │  │  │ • data/scenarios.json (트리) │  │  │   • 로컬 자립 구동 (:8002)    │
                     │  │ • 근거 문서 및 이미지 뷰어  │  │  │ • 강건성 엔진 (오타교정/구어체)│  │  │ • PyTorch CUDA 12.x 가속    │  │  │ • data/faq.json (질답 236건) │  │  │ -----------------------------│
                     │  │ • 👍/👎 만족도 피드백      │  │  │ • 되묻기 인터럽트 (HITL Clarify)│  │  └─────────────────────────────┘  │  └─────────────────────────────┘  │  │ [방법 2] GCP GPU VM 단독운영 │
                     │  └─────────────────────────────┘  │  │ • 환각 가드레일 (Grader)        │  │                                  │                                  │  │   • L4 GPU 올인원 구동 (가속) │
 ─────────────────── │                                   │  └────────────────────────────────┘  │  [ AI 모델 레이어 ]               │  [ 메타데이터 & 이력 저장소 ]    │  │   • 방화벽이슈로 80 임시개방 │
 [ 접속 채널 ]       │  [ RAG/챗봇 관리자 콘솔 (5대기능)]│                                      │  ┌─────────────────────────────┐  │  ┌─────────────────────────────┐  │  │ -----------------------------│
 ┌─────────────────┐ │  ┌─────────────────────────────┐  │  [ RAG 서브그래프 (9단계) ]          │  │ • sLLM: EXAONE / Gemma      │  │  │ • document_metadata.json       │  │  │ [향후 목표] 전용 정식운영서버│
 │ • 웹 브라우저   │ │  │ [1] FAQ 조회/등록/수정/삭제 │  │  ┌────────────────────────────────┐  │  │ • LLM: Gemini 2.5/3.1 (API) │  │  │   (요약/키워드/카테고리/발행)│  │  │   (행정망 온프레미스/전용망) │
 │ • 모바일 앱     │ │  │     (저장 시 즉시 핫리로드) │  │  │ • 다중 문서 검색 & RRF 병합   │  │  │ • Embedding: embeddinggemma │  │  │ • chat_history.db (비식별화) │  │  └─────────────────────────────┘
 │   (Flutter iOS/ │ │  │ [2] RAG 문서/전처리파이프   │  │  │ • 크로스 인코더 리랭킹 (BGE)   │  │  │ • Reranker: BGE-Reranker-v2│  │  │   (일자별 질의응답 이력)     │  │
 │    Android)     │ │  │     및 메타데이터 뷰어/편집 │  │  │ • 근거 종합 작문기 (Composer)  │  │  └─────────────────────────────┘  │  └─────────────────────────────┘  │  [ 방화벽 및 접속 포트 ]
 └─────────────────┘ │  │ [3] 웹 검색 Grounding 토글  │  │  └────────────────────────────────┘  │                                  │                                  │  ┌─────────────────────────────┐
                     │  │ [4] 버튼형 시나리오 에디터  │  │                                      │  [ 외부 검색 모델 ]               │  [ 벡터 저장소 (Vector Layer) ]  │  │ • 로컬 운영: :8002          │
 ─────────────────── │  │     (기존 시나리오 수정/등록│  │  [ LLMOps / 모니터링 ]               │  ┌─────────────────────────────┐  │  ┌─────────────────────────────┐  │  │ • GCP 운영: ⚠️ 포트 80 (임시)│
 [ 통신 프로토콜 ]   │  │ [5] 일자별 대화이력 검색    │  │  ┌────────────────────────────────┐  │  │ • Google Search Grounding   │  │  │ ⚡ FAQ 인메모리 벡터/텍스트 │  │  │ • 정식 운영 목표: HTTPS :443│
 • HTTP/HTTPS        │  │     및 개인정보 비식별화    │  │  │ • LangSmith 분산 트레이싱       │  │  │   (내부 문서 부재 시 보완) │  │  │   (data/faq.json 기반 매칭)   │  │  └─────────────────────────────┘
 • SSE (스트리밍)    │  │ • 시스템 대시보드 및 지표   │  │  │ • 일일 토큰/비용 추적          │  │  └─────────────────────────────┘  │  │   ➔ 1ms 초고속 코사인 검색 │  │
 • RESTful JSON API  │  └─────────────────────────────┘  │  │ • 런타임 엔진 무중단 핫리로드   │  │                                  │  │ 📦 RAG FlatChunkIndex         │  │  [ 백업 및 보안 감사 ]
                     │                                   │  │ • 개인정보(PII) 마스킹 필터     │  │                                  │  │   (vectors.npz + docs.json)   │  │  ┌─────────────────────────────┐
                     │  [ Web / App Proxy WAS ]          │  └────────────────────────────────┘  │                                  │  │   ➔ Kiwi BM25 + Dense Hybrid │  │  │ • ragdata/ 파싱 캐시 백업 │
                     │  ┌─────────────────────────────┐  │                                      │                                  │  │ ※ 대규모 확장용 Vector DB    │  │  │ • .env 설정 백업           │
                     │  │ • FastAPI ASGI (Uvicorn)    │  │                                      │                                  │  │   (Chroma/Qdrant - 10만건↑)│  │  │ • PII 마스킹 감사 로그     │
                     │  │ • 정적 자원 (HTML/CSS/JS)   │  │                                      │                                  │  │                                │  │                                │
                     │  └─────────────────────────────┘  │                                      │                                  │  └─────────────────────────────┘  │  └─────────────────────────────┘
=========================================================================================================================================================================
```

---

## 3. 🔍 6대 핵심 영역별 상세 구조도 및 명세 (Structural Blueprints)

### [영역 1] 사용자 및 접속 채널 (Client Layer)
```
┌────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ [접속 단말 및 환경]               [통신 채널]                           [상호작용 인터페이스]           │
│ ┌──────────────────────┐          ┌──────────────────────────┐          ┌────────────────────────────┐ │
│ │ • 학교 업무용 PC 웹   │ ───────> │ • HTTP / HTTPS REST API  │ ───────> │ • 텍스트 대화창            │ │
│ │ • 교직원 태블릿 브라우저│          │ • SSE (Server-Sent Event)│          │ • 상황별 분기 버튼         │ │
│ │ • Flutter 모바일 앱  │          │   (실시간 토큰 스트리밍) │          │ • 근거 PDF 뷰어/이미지     │ │
│ └──────────────────────┘          └──────────────────────────┘          └────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- **역할**: 교직원 및 학교 관리자가 네트워크/스쿨넷/학내망 장애 발생 시 접근하는 채널.
- **특징**: 단발성 응답 대기 지연을 방지하기 위해 **SSE(Server-Sent Events)**를 통한 실시간 처리 상태 및 타이핑 효과 제공.

---

### [영역 2] UI 및 5대 기능 통합 관리자 포털 (Portal & WAS Layer)
```
┌────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ [관리자 콘솔 5대 핵심 탭 메뉴 구조]                                                                    │
│ ┌──────────────────────┬──────────────────────┬──────────────────────┬───────────────────────────────┐ │
│ │ [탭1] FAQ 관리       │ [탭2] RAG 문서/메타  │ [탭3] 웹 검색 설정   │ [탭4] 시나리오 에디터 (신규)  │ │
│ │ • 질답 236건 CRUD    │ • PDF 업로드/삭제    │ • 실시간 On/Off 스위치│ • 기존 PPT 분기트리 시각화    │ │
│ │ • 정규화 질문 자동생성│ • 파일명 수정(Rename)│ • Gemini Grounding   │ • 노드/버튼 등록/수정/삭제    │ │
│ │ • 저장 시 즉시 핫리로드│ • LLM 요약/키워드추출│ • 일일 예산/비용 추적│ • 순환/고립 오류 무결성 검증  │ │
│ │   (수동 임베딩 불필요)│ • 5단계 재색인 콘솔  │                      │ • 런타임 트리 무중단 핫리로드 │ │
│ ├──────────────────────┴──────────────────────┴──────────────────────┴───────────────────────────────┤ │
│ │ [탭5] 대화 이력 & 비식별화 (신규)                                                                   │ │
│ │ • 일자별(캘린더 픽커) 질의응답 이력 검색 • 이름/전화/IP/계정 실시간 마스킹 • 만족도(👍/👎) 통계    │ │
│ └─────────────────────────────────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- **역할**: 관리자가 챗봇의 지식(FAQ/문서/시나리오), 연동 정책(웹검색), 감사 이력(비식별화 로그)을 통합 통제.
- **특징**: 관리자가 복잡한 수동 임베딩 명령을 실행할 필요 없이, **저장 즉시 메모리에 핫리로드(Hot-reload)**되어 챗봇 엔진에 실시간 반영.

---

### [영역 3] AI Agent 및 오케스트레이션 (Orchestration Layer)
```
┌────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ [LangGraph 메인 그래프 (14노드)]                                                                       │
│  [사용자 입력] ──> [오타/구어체 정규화] ──> [Intent 라우터] ──┬──> [1순위: 버튼 시나리오 (트리 분기)]   │
│                                                               ├──> [2순위: FAQ 의미 매칭 (1ms)]        │
│                                                               ├──> [3순위: RAG 서브그래프 (9노드)]     │
│                                                               └──> [4순위: 실시간 웹 검색 (보조)]      │
│                                                                                │                       │
│  [최종 답변 출력] <── [PII 비식별화 저장] <── [환각 검증 Grader] <── [근거 합성 작문기 (Composer)]     │
└────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- **역할**: 사용자 질의의 의도를 분석하여 최적의 답변 경로로 라우팅하고, 답변의 신뢰도를 보장.
- **특징**: 
  - **강건성 엔진**: 사투리, 구어체, 오타(예: "와파가 안대여")를 표준 질의로 자동 보정.
  - **되묻기(HITL)**: 모호한 질의 시 사용자에게 확인 선택지를 제시(인터럽트).
  - **환각 방지(Grader)**: RAG 검색 근거 문서에 기반하지 않은 거짓 답변 원천 차단.

---

### [영역 4] GPU 및 AI 모델 서빙 (Model Serving Layer)
```
┌────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ [임베딩 모델]                       [리랭커 (Reranker)]                  [생성 및 추론 LLM]             │
│ ┌──────────────────────────┐        ┌──────────────────────────┐         ┌───────────────────────────┐  │
│ │ • embeddinggemma         │        │ • BAAI/bge-reranker-v2-m3│         │ • Gemini 2.5/3.1 (API)    │  │
│ │ • 768차원 고밀도 벡터화  │        │ • 크로스 인코더 딥러닝   │         │ • sLLM (EXAONE / Gemma)   │  │
│ │ • Ollama 서빙 (로컬/GCP) │        │ • 로컬 CPU or GCP GPU    │         │ • 근거 종합 및 문장 재구성│  │
│ │   (RAG 문서 청크 벡터화) │        │   (GCP L4 가속 시 0.3초) │         │ • 문서 메타데이터 자동추출│  │
│ └──────────────────────────┘        └──────────────────────────┘         └───────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- **역할**: 고난도 벡터 연산 및 딥러닝 추론을 수행하여 실시간 대화 응답성 확보.
- **특징**: 로컬 CPU 환경에서도 동작하며, GCP GPU(L4/T4) 환경에서는 리랭킹 연산을 **0.3초**로 대폭 가속.

---

### [영역 5] 데이터 레이어 (Data & Knowledge Layer)
```
┌────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ [FAQ 저장소 & 인메모리 매칭] [RAG 하이브리드 색인]       [시나리오 트리]       [메타데이터 & 대화이력 DB] │
│ ┌────────────────────────┐  ┌───────────────────────┐   ┌─────────────────┐   ┌────────────────────────┐ │
│ │ • data/faq.json        │  │ • raw_data/documents/ │   │ • scenarios.json│   │ • document_metadata    │ │
│ │   (질답 236건 원문)    │  │ • FlatChunkIndex      │   │ • 메뉴/질문/종단│   │   .json (요약/키워드)  │ │
│ │ ➔ 정규화 텍스트 매칭   │  │   - vectors.npz (밀집)│   │   트리 매핑 노드│   │ • chat_history.db      │ │
│ │ ➔ 1ms 초고속 코사인검색│  │   - docs.json (메타)  │   │ ➔ O(1) 초고속   │   │   (PII 마스킹 암호화   │ │
│ │ ➔ 수동 임베딩 불필요   │  │   - Kiwi BM25 (희소)  │   │   상태 전이 제어│   │    일자별 인덱스 보관) │ │
│ └────────────────────────┘  └───────────────────────┘   └─────────────────┘   └────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- **역할**: 정형/비정형 지식과 실시간 대화 로그를 효율적이고 안전하게 분리 보관.
- **특징**: FAQ(236건)는 외부 DB 및 관리자 수동 임베딩 명령 없이 **경량 인메모리 매칭(1ms)**으로 초고속 처리하고, RAG는 BM25+Dense 하이브리드 인덱스 적용.

---

### [영역 6] 네트워크 및 인프라 (Infra & Security Layer) — 현행 2가지 운영 방식 vs 최종 목표
```
┌────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ [구분]               [방법 1: 로컬 PC 단독 운영]       [방법 2: GCP GPU VM 단독 운영]     [최종 목표 플랫폼]   │
├────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ • 운영 방식         │ 로컬 PC 단독 올인원 실행         │ GCP GPU VM 단독 올인원 실행      │ 전용 정식 운영 서버│
│                     │ (`run_chatbot.bat`)              │ (NVIDIA L4/T4 가속 인스턴스)     │ (행정망 온프레미스)│
│ • 네트워크 환경     │ 로컬 인트라넷/폐쇄망 지원        │ 클라우드 외부 접속 환경          │ 행정망 전용 보안망 │
│ • 접속 포트         │ 포트 :8002                       │ ⚠️ 포트 80 (통신이슈 임시개방)   │ 보안 HTTPS :443    │
│ • 가속화 수준       │ CPU 기본 연산 (20~30초)          │ GPU 60배 가속 (3~5초)            │ 전용 가속기 탑재   │
│ • 특이 사항         │ 독립 검증 및 개발 최적화         │ 실시간 외부 시연 및 고속화 최적화│ 전용 서버 배정시전환│
└────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 4. 🔄 데이터 수집·전처리·임베딩·검색 파이프라인 (Data Pipeline)

```
[ 원천 데이터 수집 ]             [ 데이터 전처리 / 정제 / 추출 ]            [ 인덱싱 및 적재 ]                  [ 런타임 검색 & 서빙 ]
┌────────────────────────┐      ┌──────────────────────────────┐      ┌──────────────────────────────┐      ┌──────────────────────────────┐
│ [경로 A: 장애 FAQ]      │ ───> │ • 특수문자/구어체 정규화     │ ───> │ • data/faq.json 저장         │ ───> │ ⚡ 런타임 메모리 즉시 로드   │
│ • data/faq.json        │      │   (question_normalized 자동) │      │   (수동 전체임베딩 불필요)   │      │   (정규화 텍스트/벡터 매칭)  │
│   (236건 질답 원문)    │      │ • 고유 ID 자동 채번          │      └──────────────────────────────┘      │   ➔ 1ms 초고속 코사인 검색   │
└────────────────────────┘      └──────────────────────────────┘                                            └──────────────────────────────┘
                                                                                                                        │ (관리자 핫리로드)
┌────────────────────────┐      ┌──────────────────────────────┐      ┌──────────────────────────────┐                  ▼
│ [경로 B: RAG 원본문서] │ ───> │ [5단계 재색인 & 메타데이터]  │ ───> │ • 청크 벡터화 (.npz)         │ ───> │ 📦 FlatChunkIndex            │
│ • raw_data/documents/  │      │ ① 문서 스캔 / PDF 파싱       │      │ • 문서 메타데이터 (.json)    │      │   (Kiwi BM25 + Dense RRF)    │
│   (학교 운영/통신 PDF) │      │ ② LLM 메타데이터 자동 추출   │      │ • Kiwi 형태소 분석 토큰화    │      │   ➔ BGE 리랭커 (0.3s)       │
└────────────────────────┘      │    (3줄요약/키워드5개/분류)  │      └──────────────────────────────┘      └──────────────────────────────┘
                                │ ③ 청킹 및 노이즈 정제       │                                                        │
                                │ ④ 위생 검증 (중복/헤더제거)  │                                                        │
                                │ ⑤ 원자적 색인 승격           │                                                        │
                                └──────────────────────────────┘                                                        ▼
┌────────────────────────┐      ┌──────────────────────────────┐      ┌──────────────────────────────┐      ┌──────────────────────────────┐
│ [경로 C: 버튼 시나리오]│ ───> │ • 시나리오 트리 구조화       │ ───> │ • data/scenarios.json        │ ───> │ 🌳 ScenarioTree 엔진         │
│ • 기존 PPT 기반 트리   │      │ • 노드/분기옵션 유효성 검증  │      │   (JSON 트리 구조 저장)      │      │   (버튼 클릭 시 O(1) 전이)   │
│   (인터넷/AP/태블릿)   │      │ • 순환 고리 / 고립 노드 검사 │      └──────────────────────────────┘      └──────────────────────────────┘
└────────────────────────┘      └──────────────────────────────┘                                                        │
                                                                                                                        ▼
┌────────────────────────┐      ┌──────────────────────────────┐      ┌──────────────────────────────┐      ┌──────────────────────────────┐
│ [경로 D: 대화 이력]    │ ───> │ • 개인정보(PII) 실시간 감지  │ ───> │ • 비식별화(마스킹) 변환      │ ───> │ 🗄️ chat_history.db           │
│ • 사용자 실시간 질의   │      │   - 성명, 연락처, IP, 계정   │      │ • SQLite 암호화 보관         │      │   (일자별 캘린더 검색 뷰어)  │
└────────────────────────┘      └──────────────────────────────┘      └──────────────────────────────┘      └──────────────────────────────┘
```

### 📊 4대 파이프라인 단계별 세부 비교 표

| 파이프라인 단계 | ① 장애 대응 FAQ (경로 A) | ② RAG 근거 문서 & 메타데이터 (경로 B) | ③ 버튼형 장애 시나리오 (경로 C) | ④ 대화 이력 & 비식별화 (경로 D) |
|---|---|---|---|---|
| **1. 원천 데이터** | `data/faq.json` (질답 236건) | `raw_data/documents/*.pdf` | `data/scenarios.json` | 실시간 사용자 세션 질의 및 응답 |
| **2. 전처리 및 정제** | 구어체 정규화 (`question_normalized`) | MinerU 텍스트 추출 + **LLM 4대 메타데이터(요약/키워드/적용대상) 추출** | 트리 구조 파싱 및 링크 유효성 검증 | **로컬 3단계 하이브리드 PII 실시간 마스킹 (Regex + Kiwi + 보조 sLLM)** |
| **3. 저장 포맷** | `faq.json` (구조화 JSON) | `vectors.npz` + `document_metadata.json` | `scenarios.json` (노드/옵션 매핑) | `chat_history.db` (마스킹 이력 + 피드백 통계) |
| **4. 런타임 검색** | **NumPy 인메모리 매칭 (1ms)** | **Kiwi BM25 + Dense RRF 융합 + BGE** | **키 선택 기반 트리 상태 전이 (O(1))** | **일자별 / 경로별 / 키워드별 인덱스 검색** |
| **5. 관리자 연계** | **CRUD (저장 시 자동 핫리로드)** | **업로드/이름변경 + 메타데이터 뷰어 + 재색인** | **기존 시나리오 수정 및 신규 등록** | **일자별 이력 조회 & 자체 만족도(👍/👎) 대시보드** |

---

## 5. 🛠️ 관리자 포털 5대 핵심 기능 상세 구조 및 화면 설계 (UI/UX Direction)

```
┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  🏫 학교 유무선 장애상담 챗봇 관리자 통합 콘솔                                                 [대화화면] [연결상태]│
├─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│  [💬 FAQ 관리]  │  [📂 RAG 문서 & 전처리]  │  [🌐 웹 검색 설정]  │  [🌳 시나리오 에디터]  │  [📊 대화이력 & 비식별화]│
└─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 1️⃣ [기능 1] 장애 대응 질문·답변 (FAQ) 관리 (CRUD 즉시 반영)
```
┌────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ [FAQ 관리 탭 화면 레이아웃]                                                                             │
│ ┌────────────────────────────────────────────────────────────────────────────────────────────────────┐ │
│ │ [통계 바] 등록 FAQ: 236건 | 스쿨넷(52) 학내망(50) 무선망(84) 관제(50)                             │ │
│ ├────────────────────────────────────────────────────────────────────────────────────────────────────┤ │
│ │ [툴바] [시트선택 ▼] [장애유형 ▼] [검색어 입력...]                                [+ 신규 FAQ 등록] │ │
│ ├────────────────────────────────────────────────────────────────────────────────────────────────────┤ │
│ │ [테이블]                                                                                           │ │
│ │ 번호 │ 분류   │ 장애유형    │ 질문 (정규화 질문)              │ 답변 요약              │ 관리        │ │
│ │ 1    │ 스쿨넷 │ 회선 불통   │ 스쿨넷이 뭐예요? 와이파이랑... │ 스쿨넷은 학교 전용... │ [수정] [삭제]│ │
│ │ 2    │ 무선망 │ AP 전원불량 │ 교실 천장 AP 불이 안 들어와요...│ POE 인젝터 전원 케이블..│ [수정] [삭제]│ │
│ └────────────────────────────────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- **CRUD 및 즉시 반영 (수동 임베딩 버튼 불필요)**:
  - 질문 등록/수정 모달: 카테고리(시트), 장애유형, 질문, 답변, 근거문서 입력.
  - 저장 시 `question_normalized` 정규화 질문 자동 생성 및 `faq.json` 안전 갱신.
  - **별도의 '전체 임베딩' 버튼을 누를 필요 없이**, 등록/수정/삭제 완료 즉시 챗봇 메모리(`SemanticScenarioMatcher`)가 핫리로드되어 실시간 대화에 바로 적용됩니다.

---

### 2️⃣ [기능 2] RAG 원본 문서 관리, 메타데이터 추출 및 전체 전처리
```
┌────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ [RAG 문서 관리 & 메타데이터 뷰어 레이아웃]                                                             │
│ ┌────────────────────────────────────────────────────────────────────────────────────────────────────┐ │
│ │ [드래그 앤 드롭 존] 📤 여기에 PDF 매뉴얼을 끌어다 놓거나 클릭하여 업로드하세요 (지침서/운영안내서)    │ │
│ ├────────────────────────────────────────────────────────────────────────────────────────────────────┤ │
│ │ [문서 목록 테이블]                                                                                 │ │
│ │ 파일명                         │ 크기   │ 페이지 │ 파싱상태 │ 메타데이터 (LLM 추출)   │ 관리       │ │
│ │ 23년_학교유무선_운영안내서.pdf │ 14.2MB │ 98쪽   │ 파싱완료 │ [#스쿨넷 #AP #10Giga...]│ [메타] [수정(이름변경)] [삭제] │
│ ├────────────────────────────────────────────────────────────────────────────────────────────────────┤ │
│ │ [사이드 드로어: 문서 메타데이터 상세 보기/수정]                                                     │ │
│ │ • 문서 요약: "본 문서는 2023년도 경기도 교육청 학교 유무선 인프라 구축 및 장애 대응 표준 지침서임"  │ │
│ │ • 핵심 키워드: [#스쿨넷, #집선청, #L2스위치, #무선AP, #장애접수]                                  │ │
│ │ • 발행기관: 한국지능정보사회진흥원(NIA) | 작성년도: 2023년 | 대상: 초·중·고 정보부장              │ │
│ ├────────────────────────────────────────────────────────────────────────────────────────────────────┤ │
│ │ [⚡ 사전 청킹 & 재색인 콘솔] [체크: 강제 전체 재파싱]  [🚀 RAG 전처리 및 재색인 시작]              │ │
│ │  (1)스캔 ➔ (2)PDF파싱 ➔ (3)메타추출/청킹 ➔ (4)임베딩 ➔ (5)색인배포 [진행률: 100%] [실시간 터미널로그]│ │
│ └────────────────────────────────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- **문서 수정(Rename)**: 파일명 변경 기능 제공 및 파싱 캐시(`ragdata/source_parsed/<slug>`) 디렉터리 자동 동기화.
- **메타데이터 자동 추출 진행 방법 및 4대 필드 정의**:
  - **추출 파이프라인**:
    1. **고속 텍스트 추출**: PDF 업로드 시 텍스트 파서(PyMuPDF / MinerU)가 문서의 표지, 목차, 서론(1~3페이지) 핵심 텍스트를 고속 추출.
    2. **정형 JSON 스키마 LLM 분석**: 경량화 고속 모델인 Gemini Flash API를 통해 4대 메타데이터 필드를 정형 JSON으로 자동 생성.
    3. **데이터베이스 적재**: 추출된 결과는 `document_metadata.json` 및 `FlatChunkIndex` 메타 필드에 자동 기록.
  - **4대 메타데이터 필드 규격**:
    - `title`: 문서 정식 명칭 (예: "2023년도 학교 유무선 인프라 운영 및 장애 대응 표준 매뉴얼")
    - `summary`: **3줄 요약문** (문서의 작성 배경, 핵심 운영 지침, 주요 장애 처리 절차 요약)
    - `keywords`: **핵심 키워드 5개** (예: `#스쿨넷`, `#L2스위치`, `#무선AP`, `#PoE인젝터`, `#장애접수`)
    - `publisher`: **발행 기관 및 작성년도** (예: 한국지능정보사회진흥원(NIA), 경기도교육청 / 2023년)
    - `target_scope` (**적용 대상**):
      - **개념**: 본 지침/매뉴얼이 유효하게 적용되는 **① 대상 주체(사용자 직책/역할)**, **② 대상 장비/인프라**, **③ 대상 공간/구역**을 명확히 규정하는 핵심 메타데이터.
      - **세부 구성**:
        - 대상 주체: 초·중·고 정보부장 교사, 일반 교직원, 행정실 전산담당자 등
        - 대상 장비: 학내망 L2/L3 스위치, 집선장비, 천장형 무선 AP, PoE 인젝터, 교원 PC, 학생 스마트 단말기 등
        - 대상 공간: 일반 교실, 컴퓨터 실습실, 교무실, 행정실 등
      - **RAG 검색 시 활용 효과**: 사용자가 "교실 천장 AP에 빨간 불이 깜빡여요"라고 질문했을 때, `target_scope`가 '무선 AP / 일반 교실'로 지정된 문서를 최우선 매칭·필터링하여 관련 없는 행정실 전산망 매뉴얼 인용을 원천 차단하고 검색 정확도를 극대화.
- **UI 반영 및 관리자 상호작용**:
  - 문서 목록의 [메타] 버튼 클릭 시 우측 사이드 드로어(Drawer)에 요약문, 키워드 배지, 적용 대상 카드가 직관적으로 렌더링.
  - 관리자는 추출된 메타데이터를 직접 검토하고 필요 시 원클릭으로 수정 및 저장 가능.
- **5단계 재색인 파이프라인**: 5단계 스테퍼 + 실시간 터미널 로그 콘솔 + 색인 원자적 교체.

---

### 3️⃣ [기능 3] 외부 실시간 웹 검색 (Google Search Grounding) ON/OFF
```
┌────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ [웹 검색 제어 카드]                                                                                    │
│ ┌────────────────────────────────────────────────────────────────────────────────────────────────────┐ │
│ │ 🌐 실시간 Google Search Grounding 보완                        [토글 스위치: ON / OFF (즉시반영)] │ │
│ │ 내부 문서(RAG) 및 FAQ로 해결 불가 시 최후의 보루로 실시간 웹 검색을 수행하여 신뢰성 있는 최신 정보 제공│ │
│ │ -------------------------------------------------------------------------------------------------- │ │
│ │ • 검색 모델: gemini-3.1-flash-lite       • 탐색 범위: in_domain_unresolved (도메인 내 미해결 건)   │ │
│ │ • 일일 호출 한도: 100회                   • 오늘 사용량/비용: 12회 / $0.02                          │ │
│ └────────────────────────────────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- **즉시 반영 & 보존**: 토글 시 런타임 Provider가 `Disabled` ↔ `GeminiGrounding`으로 핫스왑되며 `.env`에 영구 저장.

---

### 4️⃣ [기능 4] 장애 문제 버튼형 시나리오 관리 (기존 시나리오 수정 및 신규 등록)
```
┌────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ [시나리오 에디터 탭 레이아웃]                                                                          │
│ ┌───────────────────────────────────────────────┬────────────────────────────────────────────────────┐ │
│ │ [좌측: 시나리오 트리 뷰 (Visual Tree)]        │ [우측: 선택 노드 상세 편집 폼]                     │ │
│ │ 📁 root (시작 메뉴)                           │ • 노드 ID: internet_down.situation                 │ │
│ │   ├── 🔘 인터넷이 안 돼요 (internet_down)     │ • 안내 문구: "현재 상황을 알려주세요."             │ │
│ │   │     ├── 🔘 학교 전체 안 됨 (duration)     │ • 분기 옵션 버튼 목록:                             │ │
│ │   │     │     ├── 🔘 15분 이내 ➔ [종단답변]  │   [1] "학교 전체가 안 돼요" ➔ next: all.duration   │ │
│ │   │     │     └── 🔘 15분 이상 ➔ [종단답변]  │   [2] "일부 교실만 안 돼요" ➔ next: some_rooms     │ │
│ │   │     ├── 🔘 내 PC만 안 돼요 ➔ [종단답변]   │   [3] "와이파이가 안 돼요"  ➔ next: wifi.ap_light  │ │
│ │   │     └── 🔘 와이파이 안 됨                 │   [+ 버튼 추가]                                    │ │
│ │   ├── 🔘 태블릿 전원이 안 켜져요 ➔ [종단답변] │ • 종단 답변 설정 (Terminal인 경우):                │ │
│ │   └── [+ 신규 루트 시나리오 추가]             │   답변 텍스트: "스쿨넷 장애지원센터(1899-0979)..." │ │
│ ├───────────────────────────────────────────────┴────────────────────────────────────────────────────┤ │
│ │ [하단 액션] [🔍 트리 무결성 검증 (순환/고립/깨진링크 확인)]   [💾 시나리오 저장 및 런타임 핫리로드]   │ │
│ └────────────────────────────────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- **기존 시나리오 수정**: `data/scenarios.json`의 기존 9개 주요 장애 트리(인터넷 불통, 속도 저하, AP 무선 장애, 테블릿 전원/연결 장애 등)를 직관적으로 수정.
- **무결성 검사**: 순환 참조, 미존재 노드 링크, 답변 누락을 저장 전 사전 차단하여 챗봇 먹통 오류 방지.

---

### 5️⃣ [기능 5] 사용자 대화 이력 일자별 검색, PII 비식별화 및 답변 만족도 통합 집계 (Phase 3)
```
┌────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ [대화 이력 검색 & 비식별화 감사 레이아웃]                                                               │
│ ┌────────────────────────────────────────────────────────────────────────────────────────────────────┐ │
│ │ [검색 필터] 기간: [2026-09-01] ~ [2026-09-09]  경로: [전체 ▼]  만족도: [전체 ▼]  검색어: [인터넷...]│ │
│ ├────────────────────────────────────────────────────────────────────────────────────────────────────┤ │
│ │ [이력 목록 테이블]                                                                                 │ │
│ │ 일시             │ 세션ID │ 사용자 질문 (PII 마스킹)          │ 처리경로 │ 소요시간 │ 피드백 │ 상세│ │
│ │ 2026-09-09 14:10 │ a1b2c3 │ 교무실 홍*동 교사 PC IP 192...    │ FAQ의미  │ 0.05초   │ 👍     │ [보기]│ │
│ │ 2026-09-09 13:45 │ d4e5f6 │ 010-****-5678로 연락 부탁드려요...│ RAG문서  │ 0.42초   │ -      │ [보기]│ │
│ ├────────────────────────────────────────────────────────────────────────────────────────────────────┤ │
│ │ [세션 상세 대화 타임라인 모달]                                                                     │ │
│ │ • 원본 질의: "교무실 홍*동 교사 PC IP 192.168.*.* 인터넷이 안 됩니다. 010-****-1234 연락요망"     │ │
│ │ • 판단 근거: FAQ ID '스쿨넷:15' (유사도 0.88) ➔ BGE 리랭킹 1위                                      │ │
│ │ • 최종 응답: "교무실 고정 IP 충돌 문제일 수 있습니다. 네트워크 어댑터 설정에서..."                  │ │
│ └────────────────────────────────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- **개인정보 비식별화 하이브리드 파이프라인 (LLM 단독 배제)**:
  - **LLM 단독 처리 배제 사유**: 대화 이력 비식별화를 온전히 LLM에만 의존할 경우, ① 매 대화마다 1~2초의 불필요한 API 지연 발생, ② 호출 비용 누적, ③ 민감 원본 정보(실명, 전화번호, IP)가 외부 클라우드로 전송되는 보안 리스크, ④ LLM의 확률적 특성으로 인한 간헐적 마스킹 누락(환각) 위험이 발생합니다.
  - **하이브리드 3단계 고속 비식별화 엔진**:
    1. **1단계: 초고속 정규표현식(Regex) 규칙 엔진 (로컬 0.001초)**:
       - 전화번호: `010-1234-5678` ➔ `010-****-5678` (가운데 4자리 마스킹)
       - IP 주소: `192.168.1.50` ➔ `192.168.*.*` (호스트 대역 마스킹)
       - 주민등록번호/계정패스워드: 원천 차단 마스킹 (`******-*******`)
       - 정형 패턴의 개인정보를 로컬에서 100% 무누락으로 즉시 차단.
    2. **2단계: 한국어 형태소 분석기(Kiwi) 기반 인명·호칭 문맥 분석 (로컬 0.01초)**:
       - "홍길동 선생님", "김철수 주무관" 등 문맥 내 고유명사(인명)와 직책·호칭 명사를 형태소 태깅으로 결합 감지하여 성명 가명화 (`홍*동 교사`).
    3. **3단계: 복합 비정형 문맥 보조 (선택적 경량 로컬 sLLM)**:
       - 규칙으로 판별하기 어려운 복합 사생활 정보가 포함된 경우에 한해 내부 경량 모델로 보조 마스킹.
    - **결과**: 외부 통신 없이 **로컬에서 0.02초 이내, 비용 0원, 데이터 유출 제로(Zero)** 상태로 안전하게 비식별화 완료 후 `chat_history.db`에 적재.
- **답변 만족도(👍/👎) 통합 집계 계획 (대화 히스토리 기능 구현 시 동시 구축)**:
  - **구현 시점 및 연계**: **답변 만족도 집계는 별도 기능으로 선행하지 않고, 향후 본 5번 기능(대화 히스토리 관리) 개발 시점에 세션 질의응답 레코드와 결합하여 함께 집계 가능하도록 일체형으로 구축**합니다.
  - **동시 집계 구조**: `chat_history.db` 테이블의 `feedback` 컬럼(POSITIVE / NEGATIVE / NONE)을 통해 질의 내용, 마스킹 결과, AI 추론 경로, 사용자 평가를 단일 트랜잭션으로 관리.
  - **통계 대시보드 위젯 제공**:
    - 대화 이력 탭 상단에 기간별 전체 만족도 추이(긍정률/부정률) 그래프 렌더링
    - 처리 경로별(FAQ, RAG, 시나리오, 웹검색) 품질 비교 통계
    - **부정 피드백(👎) 발생 건 원클릭 필터링**: 불만족 응답 건의 원본 질의와 챗봇 판단 경로를 즉시 확인하여 FAQ 추가 및 RAG 보완에 직결.

---

## 6. 💡 데이터 레이어: FAQ 인메모리 벡터 vs Vector DB 비교 분석

| 비교 항목 | 현재 채택 방식: 인메모리 벡터 행렬 (`self._mat`) | 전통적 Vector DB (Chroma, Milvus, Qdrant 등) |
|---|---|---|
| **동작 원리** | 질문 벡터들을 메모리에 올려두고 **NumPy 행렬곱**으로 브루트포스 코사인 계산 | 별도 DB 프로세스를 띄우고 **HNSW/IVF 인덱스 트리**를 통해 근사 최근접 탐색 |
| **검색 속도** | **1ms 이내 (0.001초)** — IPC/네트워크 통신 오버헤드가 전혀 없음 | **10 ~ 30ms** — 네트워크 호출 및 DB 쿼리 파싱 오버헤드 수반 |
| **최적 데이터 규모** | **수백 건 ~ 5만 건 이하** (현재 FAQ 236건에 가장 적합) | **10만 건 ~ 수백만 건 이상** (대규모 빅데이터 환경) |
| **시스템 의존성** | **Zero Dependency** — 파이썬 기본 환경과 NumPy 외 추가 데몬 불필요 | DB 서버 프로세스 상시 구동 및 버전/포트/네트워크 관리 필요 |
| **인덱스 안정성** | 세그먼트 충돌이나 파일 잠금(Lock) 에러 없음 | 프로세스 비정상 종료 시 HNSW 세그먼트 손상 위험 존재 |
| **갱신 방식** | **등록/수정 시 시스템 자동 핫리로드** (수동 작업 불필요) | **단건 Upsert/Delete 실시간 색인** 가능 |

---

## 7. 🌟 생성형 AI 플랫폼 핵심 7대 요소 요약

```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│                                 챗봇 시스템 핵심 요소                                   │
├────────────────────────────────────────────────────────────────────────────────────────┤
│  [1] 초경량 고속 인메모리 벡터 (In-Memory Vector Matrix)                                │
│      • 외부 DB 데몬 없이 NumPy 행렬로 1ms 초고속 코사인 유사도 검색 보장               │
├────────────────────────────────────────────────────────────────────────────────────────┤
│  [2] BGE 리랭커 & GCP GPU 가속 (Cross-Encoder Reranking)                                │
│      • 1차 검색 후보군을 BGE-M3 리랭커로 재정렬 (GCP L4 GPU로 22초 ➔ 0.3초 가속)       │
├────────────────────────────────────────────────────────────────────────────────────────┤
│  [3] LangGraph 멀티 에이전트 & 가드레일 (Multi-Agent & Guardrails)                     │
│      • 14노드 오케스트레이션, 되묻기(HITL), 답변 근거 검증(Grader)으로 환각 원천 차단 │
├────────────────────────────────────────────────────────────────────────────────────────┤
│  [4] 실시간 웹 검색 상호 보완 (Google Search Grounding)                                │
│      • 내부 매뉴얼로 해결 불가 시 관리자 토글(On/Off)로 Gemini 웹 검색 즉시 보완       │
├────────────────────────────────────────────────────────────────────────────────────────┤
│  [5] 관리자 무중단 핫리로드 (Zero-Downtime Hot-Reload)                                  │
│      • FAQ/시나리오 변경 및 저장 시 서버 재시작 없이 메모리 인스턴스 즉각 갱신         │
├────────────────────────────────────────────────────────────────────────────────────────┤
│  [6] 문서 메타데이터 자동 추출 및 시각화 (Document Metadata Extraction)                │
│      • RAG 업로드 문서의 핵심 키워드, 3줄 요약, 카테고리를 LLM으로 자동 추출/관리      │
├────────────────────────────────────────────────────────────────────────────────────────┤
│  [7] 개인정보(PII) 비식별화 대화 이력 & 만족도 통합 저장소 (History & Analytics)        │
│      • 대화 저장 시 이름/전화/IP 실시간 마스킹 암호화 및 답변 만족도(👍/👎) 동시 집계   │
└────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 8. 🚀 단계별 구현 로드맵 (Phased Implementation Roadmap)

```
[ 1단계: 우선 구현 과제 (Phase 1 - 즉시 착수) ]
  ├── 1. 장애 대응 FAQ 관리 (조회/등록/수정/삭제 CRUD 및 저장 시 자동 핫리로드)
  ├── 2. RAG 근거 문서 관리 (업로드, 파일명 수정 Rename, 삭제, 5단계 재색인 콘솔)
  └── 3. 외부 웹 검색 Grounding 실시간 On/Off 제어 및 예산 모니터링
      ▼
[ 2단계: 시나리오 및 문서 지능화 (Phase 2) ]
  ├── 4. 장애 문제 버튼형 시나리오 트리 관리자 (기존 트리 시각화/수정, 노드/옵션 관리, 무결성 검사)
  └── 5. RAG 문서별 LLM 메타데이터(3줄 요약/키워드 5개/적용대상) 자동 추출 및 사이드 뷰어/편집기
      ▼
[ 3단계: 대화 이력 관리, PII 비식별화 및 답변 만족도 동시 집계 (Phase 3) ]
  └── 6. 사용자 질의응답 이력 일자별 검색 대시보드 구축
         ├── 🛡️ PII(성명/연락처/IP) 3단계 하이브리드 실시간 비식별화 엔진
         └── 📊 답변 만족도(👍/👎) 동시 집계 엔진 (대화 이력 DB 적재 시 피드백 통합 집계)
```
