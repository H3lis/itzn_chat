# Antigravity AI Agent 필수 준수 2대 핵심 수칙

본 프로젝트(`H3lis/itzn_chat`)에서 작업하는 모든 AI 에이전트는 사용자의 확정 지침에 따라 아래 2대 수칙을 100% 무조건 준수해야 합니다. 컴퓨터를 재부팅하거나 세션이 새로 열리더라도 예외 없이 최우선 적용됩니다.

---

## 1. 📝 [수칙 1] 모든 작업 내역의 인수인계 MD 실시간 영구 기록
- **대상 파일**: [`작업인수인계_및_진행현황.md`](file:///c:/Users/ITZN/Desktop/챗봇/작업인수인계_및_진행현황.md)
- **행동 지침**:
  - 세션이 시작되면 **무조건 본 인수인계 문서를 가장 먼저 읽고** 현재 시스템 상태와 이전 작업 히스토리를 파악할 것.
  - 코드 변경, 버그 수정, 빌드, 서버 배포, 설정 변경 등 **모든 작업 단계가 완료될 때마다 실시간으로 해당 문서에 상세 내역을 누락 없이 업데이트**할 것.

---

## 2. 🚀 [수칙 2] 작업 완료 시 GitHub 자동 Commit & Push 필수 이행
- **대상 원격지**: `https://github.com/H3lis/itzn_chat.git` (브랜치: `main`)
- **행동 지침**:
  - 로컬에서 코드, 프론트엔드 빌드 번들(`frontend/dist`), 설정 파일 등을 수정했을 때, **사용자가 따로 지시하지 않아도 작업 완료 즉시 자동으로 `git add`, `git commit`, `git push origin main`을 실행**하여 GitHub와 100% 동기화할 것.
  - 이를 통해 GCP L4 GPU 서버(`34.64.143.198`)에서 언제든 `~/update.sh` (git pull & restart) 한 줄로 0.5초 만에 즉시 최신 버전이 반영되도록 상시 보장할 것.

---

## 3. 🛡️ [보안 수칙] 개인정보(PII) 비식별화 DB 무누락 적재
- 실명(단독 인명 포함), 연락처(대시 없는 번호 포함), IP/MAC, 기기 시리얼 등 개인식별가능정보는 `chat_history.db`에 저장되기 전 100% 비식별화 마스킹을 거쳐야 함.
