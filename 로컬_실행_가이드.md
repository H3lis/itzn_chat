# 📖 학교 유무선 장애상담 챗봇 (chatbot_demo_v2) 로컬 실행 가이드

본 문서는 현재 PC 환경에 맞추어 구성된 **가상환경 정보, 활성화 방법, 챗봇 서버 실행 및 테스트 작동법**을 정리한 가이드입니다.

---

## 1. 🐍 가상환경 기본 정보

| 항목 | 상세 정보 |
|---|---|
| **가상환경 이름** | `.venv` |
| **가상환경 경로** | `c:\Users\ITZN\Desktop\챗봇\.venv` |
| **Python 버전** | `Python 3.11.9 (64-bit)` |
| **Python 실행기** | `.venv\Scripts\python.exe` |
| **Pip 패키지 관리자** | `.venv\Scripts\pip.exe` |

---

## 2. ⚡ 원클릭 실행 방법 (가장 쉬운 방법)

별도의 터미널 명령어 입력 없이 폴더 내 배치 파일을 더블클릭하여 바로 실행할 수 있습니다.

- **챗봇 서버 실행**: [`run_chatbot.bat`](run_chatbot.bat) 더블클릭  
  → 자동으로 가상환경을 사용하여 `http://127.0.0.1:8002` 서버가 시작됩니다.
- **전체 테스트 실행**: [`run_tests.bat`](run_tests.bat) 더블클릭  
  → 191개 단위/통합 테스트를 실행하여 정상 작동 여부를 검증합니다.

---

## 3. 💻 터미널(CLI) 작동법

### 가상환경 활성화 (Activate)

#### 1) 명령 프롬프트 (CMD)
```cmd
cd /d "c:\Users\ITZN\Desktop\챗봇"
.venv\Scripts\activate.bat
```
> 프롬프트 앞에 `(.venv)` 표시가 나타나면 정상 활성화된 상태입니다.

#### 2) PowerShell
```powershell
cd "c:\Users\ITZN\Desktop\챗봇"
.\.venv\Scripts\Activate.ps1
```
> ⚠️ PowerShell 스크립트 실행 오류 발생 시 아래 명령어를 먼저 한 번 실행하세요:  
> `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass`

#### 3) 가상환경 종료 (Deactivate)
```cmd
deactivate
```

---

### 챗봇 서버 실행 및 접속

#### 가상환경 활성화 상태에서 실행
```cmd
python -m chatbot_demo_v2 --port 8002
```

#### 가상환경 활성화 없이 직접 실행
```cmd
.venv\Scripts\python.exe -m chatbot_demo_v2 --port 8002
```

- **웹 브라우저 접속**: [http://127.0.0.1:8002](http://127.0.0.1:8002)
- **포트 변경 실행**: 뒤의 포트 번호를 변경하여 실행 가능 (예: `--port 8003`)
- **서버 종료**: 터미널 창에서 `Ctrl + C` 입력

---

## 4. 🧪 테스트 실행 및 검증

코드 수정이나 데이터 변경 후 시스템 무결성을 테스트할 수 있습니다.

```cmd
:: 전체 테스트 (외부 API/GPU 없이 오프라인 검증)
.venv\Scripts\python.exe -m pytest chatbot_demo_v2/tests -q

:: 특정 테스트 파일만 실행
.venv\Scripts\python.exe -m pytest chatbot_demo_v2/tests/test_robustness.py -v
```

---

## 5. ⚙️ 환경 설정 (.env) 안내

설정 파일 위치: [`chatbot_demo_v2/.env`](chatbot_demo_v2/.env)

| 환경변수 | 기본값 | 설명 |
|---|---|---|
| `GEMINI_API_KEY` | *(미설정)* | Gemini API 키. 설정 시 RAG 답변 생성, 답변 합성(Composer), 채점(Grader)에 LLM이 활성화됩니다. |
| `RAG_BACKEND` | `gemini` | RAG 백엔드 선택 (`gemini` 또는 `ollama`) |
| `DEMO_PORT` | `8002` | 기본 실행 포트 번호 |
| `SCENARIO_MATCH_BACKEND` | `semantic` | FAQ 유사도 매칭 방식 (`semantic`: BGE 리랭커+임베딩, `fuzz`: 문자 편집거리) |
| `ROBUSTNESS_TOOL_ENABLED` | `true` | 구어체 및 오타 강건성 엔진 활성화 여부 |
| `WEB_SEARCH_ENABLED` | `false` | Google 검색 그라운딩 활성화 여부 (유료 Gemini API 키 필요) |

> 💡 **참고**: `GEMINI_API_KEY`나 로컬 `Ollama`가 없더라도, **시나리오 트리 버튼 선택, FAQ 모범 답변 안내, 오타 자동 교정** 등의 핵심 기능은 오프라인 상태에서도 완벽히 작동합니다.

---

## 6. 📁 프로젝트 주요 디렉토리 구조

```
챗봇/
├── .venv/                         # Python 3.11 가상환경
├── run_chatbot.bat                # [원클릭] 챗봇 서버 실행
├── run_tests.bat                  # [원클릭] 전체 테스트 실행
├── 로컬_실행_가이드.md             # 본 가이드 문서
│
├── chatbot_demo_v2/               # 메인 챗봇 v2 패키지
│   ├── .env                       # 환경 설정 파일
│   ├── app/                       # FastAPI 웹 서버 및 라우터
│   ├── graph/                     # LangGraph 상태 그래프 및 노드 정의
│   ├── scenario/                  # 시나리오 트리 및 FAQ 매처
│   ├── robustness_tool/           # 한글 오타 교정 및 구어체 처리 엔진
│   ├── rag/ & ragcore/            # RAG3X 검색 및 서브그래프 모듈
│   ├── data/                      # FAQ, 시나리오, 임베딩 데이터
│   ├── static/                    # 웹 UI 정적 리소스 (HTML/CSS/JS)
│   └── tests/                     # 단위/통합 테스트 (191개 테스트)
│
├── test_1/, test_2/, test_3/      # RAG 연구 및 실험 데이터
└── ask_compare_cli.py             # 모델 비교 CLI 도구
```
